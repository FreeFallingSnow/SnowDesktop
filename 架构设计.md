# SnowDesktop 拖拽系统 OO 架构设计

## 一、设计目标

消除 `app.h` 中过程式 `if/else` + `switch(ctype)` 分支，改为多态分发。每个类自己负责自己的行为。

## 二、类层次总览

```
Item（可拖拽实体，负责自渲染和 OLE）
├── DesktopIcon : Item          ← 包装 DesktopItem
│
├── Widget : Item, Container    ← 组件既是 Item（可拖）也是 Container（可放）
│   ├── Collection : Widget
│   ├── FileCategories : Widget
│   ├── FolderMapping : Widget
│   └── LuaScript : Widget     ← GetSlots() 返回空，Drop 操作空实现

Container（可放置 Item 的东西，负责 BuildSlots + OnItemsDropped）
├── DesktopGrid : Container     ← 桌面网格
│
└── Widget（通过多重继承也是 Container）

Slot（Container 内的一个位置）
├── item: Item*                 ← null = 空 slot
├── GetDropZones()              ← 空 → [EmptyDropZone]，占 → [SortZone, HandoffZone]

DropZone（落点，负责提示+执行+渲染插入线）
├── SortDropZone : DropZone     ← "重新排序" / VBar/HBar
├── HandoffDropZone : DropZone  ← "交给xx处理" / 不画线
└── EmptyDropZone : DropZone    ← "移动到此空位" / Placeholder

DragSource（拖拽源，负责收集选中项+偏移绘制）
├── DesktopDragSource : DragSource  ← items_ 中选中的 DesktopItem
└── WidgetDragSource : DragSource   ← folderEntries 中选中的条目

DragOperation（一次拖拽流程，不继承）
├── source_: DragSource*
├── currentZone_: DropZone*
├── OnMove(pt) / OnDrop(pt) / OnPaint(ctx)
```

## 三、核心接口定义

### 3.1 Item

```cpp
class Item {
public:
    virtual ~Item() = default;
    virtual std::wstring GetTitle() const = 0;         // 显示名
    virtual std::wstring GetPath() const = 0;           // 文件系统路径
    virtual HBITMAP GetIconBitmap() const = 0;          // 图标
    virtual RECT GetBounds() const = 0;                 // 当前位置
    virtual bool IsSelected() const = 0;
    virtual Container* GetContainer() const = 0;        // 所属容器（null=桌面）

    // 渲染自己到指定矩形 + 状态
    virtual void Draw(ID2D1DeviceContext*, RECT rect, int state) = 0;
    // state: 0=normal, 1=hovered, 2=selected, 3=dragged

    // OLE
    virtual ComPtr<IDataObject> CreateDataObject() = 0;
};
```

**DesktopIcon**：持有 `DesktopItem*`（非拥有），从 `DesktopItem` 的 name/pidl/bitmap 取值。

**决策点**：DesktopItem 和 FolderEntry 不改成继承 Item。原因：(1) 它们包含 PIDL/Shell 耦合，移动/拷贝操作复杂；(2) 改布局会影响 JSON 序列化。用适配器包装：`DesktopIcon : Item` 持 `DesktopItem*`，`FolderEntryIcon : Item` 持 `FolderEntry*`。

### 3.2 Container

```cpp
class Container {
public:
    virtual ~Container() = default;
    virtual std::wstring GetTitle() const = 0;

    // 布局
    virtual std::vector<std::unique_ptr<Slot>> BuildSlots() = 0;
    BarStyle GetInsertionStyle() const;

    // 拖入响应（双分派：目标容器决定如何处理来自源容器的 drop）
    virtual void OnItemsDropped(DragSource* source, Slot* targetSlot,
        DropZone* zone, int mods) = 0;

    // 渲染
    virtual void DrawChrome(ID2D1DeviceContext*, POINT mousePt) {}
    virtual RECT GetBounds() const = 0;
};
```

**DesktopGrid**：持有 `std::vector<GridPage>*` 和 `std::vector<DesktopItem>*`（引用 app 数据）。`BuildSlots()` 遍历所有页面所有 cell。

**Widget 子类**：
- `Collection::OnItemsDropped` → 调用 `app->MoveSelectedItemsToCollection`
- `FileCategories::OnItemsDropped` → 调用 `app->AddSelectedItemsToFileCategoryWidget`
- `FolderMapping::OnItemsDropped` → 同源重排 `FinishWidgetMemberReorder`，异源 `CopyFilesToFolder` + `Refresh`

**这里没有 switch(ctype)**——每个子类覆写自己的 `OnItemsDropped`。

### 3.3 Slot

```cpp
class Slot {
public:
    Slot(Container* parent, RECT bounds, size_t index);
    Container* GetParent() const;
    RECT GetBounds() const;
    Item* GetItem() const;
    void SetItem(Item*);       // null = 清空
    size_t GetIndex() const;
    bool IsEmpty() const;
    RECT GetIconRect() const;  // 64x64 居中 + top 6px

    // 空 → [EmptyDropZone]，有 Item → [SortDropZone, HandoffDropZone]
    std::vector<std::unique_ptr<DropZone>> BuildDropZones();
};
```

`GetIconRect()` 计算逻辑与现有 `GetItemIconRect(bounds)` 完全一致。

### 3.4 DropZone

```cpp
class DropZone {
public:
    DropZone(Slot* slot, RECT bounds);
    virtual ~DropZone() = default;

    Slot* GetSlot() const;
    RECT GetBounds() const;
    Container* GetContainer() const;  // slot->GetParent()

    virtual std::wstring GetHint(const DragSource* source, int mods) const = 0;
    virtual void Execute(DragSource* source, int mods) = 0;
    virtual void DrawIndicator(ID2D1DeviceContext*) const = 0;
};

class SortDropZone : public DropZone {
    // 提示："重新排序"（组件内）或"该位置已被占用"（桌面）
    // 执行：委托 Container::OnItemsDropped
    // 绘制：VBar（图标模式）或 HBar（列表模式）
};

class HandoffDropZone : public DropZone {
    // 提示："交给「xx」处理"
    // 执行：ShellExecuteEx / OLE 拖放
    // 绘制：不画
    // bounds = slot.iconRect + 4px padding
};

class EmptyDropZone : public DropZone {
    // 提示："移动到此空位" / "加入集合「xx」"
    // 执行：委托 Container::OnItemsDropped
    // 绘制：Placeholder（蓝色虚线填充整格）或 HBar
};
```

**没有 switch(kind)**——每个子类覆写 `Execute`。

### 3.5 DragSource

```cpp
class DragSource {
public:
    virtual ~DragSource() = default;
    virtual std::vector<Item*> GetItems() const = 0;
    virtual ComPtr<IDataObject> CreateDataObject() = 0;
    virtual void DrawDragged(ID2D1DeviceContext*, POINT offset) = 0;
    virtual void OnDragEnd() = 0;
    virtual Container* GetOriginContainer() const = 0;
};

class DesktopDragSource : public DragSource {
    // GetItems → items_ 中选中的 DesktopIcon
    // DrawDragged → 逐个 Item::Draw(state=dragged, offset)
    // CreateDataObject → 现有 CreateSelectedDataObject 逻辑
};

class WidgetDragSource : public DragSource {
    // GetItems → folderEntries 中选中的 FolderEntryIcon
    // DrawDragged → 同上
    // CreateDataObject → 现有 CreateFolderEntriesDataObject 逻辑
};
```

### 3.6 DragOperation

```cpp
class DragOperation {
    std::unique_ptr<DragSource> source_;
    DropZone* currentZone_ = nullptr;
    POINT startPt_, currentPt_;
public:
    DragOperation(std::unique_ptr<DragSource> source, POINT startPt);

    void OnMove(POINT pt, const std::vector<std::unique_ptr<Container>>& containers);
    // 遍历 Container→Slot→DropZone，PtInRect 命中 → currentZone_

    void OnDrop(POINT pt, int mods);
    // currentZone_->Execute(source_.get(), mods)

    void OnPaint(ID2D1DeviceContext*);
    // source_->DrawDragged(offset) + currentZone_->DrawIndicator()

    const std::wstring& GetHint() const;
};
```

## 四、app.h 接入（最小化改动）

```cpp
class SnowDesktopApp {
    // 新成员
    std::vector<std::unique_ptr<Container>> containers_;  // DesktopGrid + 每个 Widget
    std::vector<std::unique_ptr<Item>> items_;            // 所有 DesktopIcon + FolderEntryIcon
    std::unique_ptr<DragOperation> dragOp_;
    std::vector<std::unique_ptr<DesktopItem>> desktopItems_;  // 原有 data
    std::vector<std::unique_ptr<DesktopWidget>> widgets_;     // 原有 data

    void RebuildContainersAndItems();  // LayoutItems 末尾调用

    void OnMouseDown(POINT pt) {
        auto* item = FindItemAt(pt);
        if (item) {
            auto source = item->GetContainer()
                ? std::make_unique<WidgetDragSource>(...) 
                : std::make_unique<DesktopDragSource>(...);
            dragOp_ = std::make_unique<DragOperation>(std::move(source), pt);
        }
    }

    void OnMouseMove(POINT pt) {
        if (dragOp_) dragOp_->OnMove(pt, containers_);
    }

    void OnLeftButtonUp(POINT pt) {
        if (dragOp_) dragOp_->OnDrop(pt, GetModifiers());
    }

    void OnPaint(ID2D1DeviceContext* ctx) {
        for (auto& c : containers_) c->DrawContents(ctx);  // Container + Slot 框架
        for (auto& item : items_) {
            if (item->IsSelected() && dragOp_) continue;   // 拖拽中跳过原位
            item->Draw(ctx, item->GetBounds(), item->IsSelected() ? 2 : 0);
        }
        if (dragOp_) dragOp_->OnPaint(ctx);
    }
};
```

## 五、文件清单

| 文件 | 内容 | 行数估 |
|---|---|---|
| `src/item.h` | Item 基类 + DesktopIcon + FolderEntryIcon 包装器 | ~80 |
| `src/container.h` | Container 基类 | ~40 |
| `src/slot.h` | Slot | ~50 |
| `src/dropzone.h` | DropZone 基类 + Sort/Handoff/Empty 子类 | ~80 |
| `src/drag_source.h` | DragSource 基类 + Desktop/Widget 子类 | ~60 |
| `src/drag_op.h` | DragOperation | ~50 |
| `src/desktop.h` | DesktopGrid : Container | ~40 |
| `src/widget.h` | Widget 基类 + Collection/FileCategories/FolderMapping/LuaScript 声明 | ~80 |
| `src/widgets/collection.cpp` | Collection::BuildSlots + OnItemsDropped | ~80 |
| `src/widgets/file_categories.cpp` | FileCategories::BuildSlots + OnItemsDropped | ~80 |
| `src/widgets/folder_mapping.cpp` | FolderMapping::BuildSlots + OnItemsDropped | ~150 |
| `src/widgets/lua_script.cpp` | LuaScript 空实现 | ~30 |
| **修改** | | |
| `src/app.h` | 删旧代码，接入新系统 | -800 / +100 |
| `CMakeLists.txt` | 加新文件 | +13 |
| **总计新增** | | ~860 行 |

## 六、关键设计决策

### 6.1 Item 用包装器而非继承
`DesktopItem` 和 `FolderEntry` 保持原样（PIDL/Shell 耦合不改），用 `DesktopIcon`/`FolderEntryIcon` 包装。优势：不影响 JSON 序列化、Shell 操作、GDI 渲染。

### 6.2 Container 子类负责自己的文件操作
`FolderMapping::OnItemsDropped` 直接调用 `CopyFilesToFolder`、`FinishWidgetMemberReorder` 等——不需要外部 switch。这些函数作为 app.h 的 public helper 暴露给子类。

### 6.3 DragSource 分两个子类就够
`DesktopDragSource` 处理桌面图标 / C/F 条目（都是 items_ 中的 DesktopItem），`WidgetDragSource` 处理 M 条目（folderEntries）。不需要第三个。

### 6.4 渲染 OO 化

每帧渲染路径：

```
OnPaint:
  for each Container c:
    c->DrawChrome(ctx, mousePt)       ← 滚动条、切换按钮、标题栏
    for each Slot s in c->GetSlots():
      if Item* item = s->GetItem():
        item->Draw(ctx, s->GetBounds(), state)  ← 图标+文本+选中高亮
  if dragOp_:
    dragOp_->source_->DrawDragged(ctx, offset)    ← 选中 Item 偏移绘制
    dragOp_->currentZone_->DrawIndicator(ctx)     ← 插入指示线
```

各对象的渲染方法：

| 对象 | 方法 | 渲染内容 | 实现来源 |
|---|---|---|---|
| `Item` | `Draw(ctx, rect, state)` | 图标+文本+选中高亮+hover效果 | 复用现有 `DrawD2DItemGeneric` / `DrawD2DItemGenericList` |
| `Container` | `DrawChrome(ctx, mousePt)` | 每个子类不同 | 见下表 |
| `Container` | `DrawContents(ctx)` | 遍历 Slot 调 Item::Draw | 基类实现 |
| `DragSource` | `DrawDragged(ctx, offset)` | 逐个 Item::Draw(offset=拖拽偏移) | 基类实现，子类提供 Item 列表 |
| `DropZone` | `DrawIndicator(ctx)` | 插入线 | 子类各自实现 |

**DrawChrome 各子类：**

| Container 子类 | DrawChrome 内容 |
|---|---|
| `DesktopGrid` | 页面导航按钮 |
| `Collection` | 列表/图标切换按钮、标题栏、渐变底栏 |
| `FileCategories` | 分类 Tab 标签栏、列表/图标切换按钮 |
| `FolderMapping` | 列表/图标切换按钮、打开源文件夹按钮、标题栏、渐变底栏、滚动条 |
| `LuaScript` | 什么都不画（Lua 自渲染） |

**DrawIndicator 各子类：**

| DropZone 子类 | 渲染 |
|---|---|
| `SortDropZone` | VBar（图标模式，竖杠在 item left/right）或 HBar（列表模式，横杠在行 top/bottom） |
| `EmptyDropZone` | Placeholder（桌面→蓝色虚线整格）或 HBar（组件→横杠） |
| `HandoffDropZone` | 不画 |

**Item::Draw 的 state 参数**：
- `state=0 Normal` — 标准图标+文字
- `state=1 Hovered` — 加半透明 hover 背景
- `state=2 Selected` — 灰色半透明选中框（icon+text 区域）
- `state=3 Dragged` — 选中框+半透明（拖拽中跟鼠标的那个）

**现有渲染函数复用**：
- `DrawD2DItemGeneric` / `DrawD2DItemGenericList` → 移入 `Item::Draw`
- `DrawD2DScrollbar` → 移入 `Container::DrawChrome`（FolderMapping）
- `DrawD2DRoundedRectangle` / `DrawD2DFilledRectangle` → 保留为工具函数
- `GetItemIconRect` / `GetItemTextRect` → 移入 `Slot` 或 `Item`

### 6.5 Slot 每次 BuildSlots 重建
Slot 是轻量对象（RECT + 指针），每次 `BuildSlots()` 重建开销 < 1ms。不需要缓存/增量更新。

## 七、与现有代码的对照迁移

| 现有过程式代码 | 迁移到 |
|---|---|
| `DesktopHitKind` + `HitTestDesktop()` | `Container::BuildSlots()` → `Slot.bounds` → `PtInRect` |
| `MakeInternalDragHint()` 的 if/else | 各 DropZone 子类的 `GetHint()` |
| `OnMouseMove` 内联 150 行 | `DragOperation::OnMove()` |
| `OnLeftButtonUp` 内联 200 行 | `DragOperation::OnDrop()` → `DropZone::Execute()` → `Container::OnItemsDropped()` |
| `DrawD2DDraggedItems()` | `DragSource::DrawDragged()` |
| `DrawD2DWidgetMemberInsertionPreview()` 等 | `DropZone::DrawIndicator()` |
| `Container::OnDrop()` 里 switch(ctype) | 每个 Container 子类覆写 `OnItemsDropped()` |
| `DropZone::Exec()` 里 switch(kind) | 每个 DropZone 子类覆写 `Execute()` |

---

## 八、拖拽行为矩阵（OO 设计视角）

> 每行标注该场景涉及的核心类 → 方法，展示多态分发路径。
> 修饰键统一为 Windows 系统行为：默认=同盘移动/异盘复制 | Ctrl=复制 | Alt=快捷方式 | 容器内排序不适用修饰键

### 8.1 D → D（桌面图标 → 桌面）

| # | 子场景 | 触发链 | 提示文字 | 执行链 |
|---|---|---|---|---|
| D→D-1 | 空 cell | Slot(空) → `EmptyDropZone::GetHint()` | "移动到此空位" | `EmptyDropZone::Execute()` → `DesktopGrid::OnItemsDropped()` → `MoveToCell` |
| D→D-2 | 有图标，icon 外 | Slot(占) → `SortDropZone::GetHint()` | "该位置已被占用" | `SortDropZone::Execute()` → `DesktopGrid::OnItemsDropped()` → `FindBestDropCell` 就近放置 |
| D→D-3 | 有图标，icon 上 | Slot(占) → `HandoffDropZone::GetHint()` | "交给「xx」处理" | `HandoffDropZone::Execute()` → `ShellExecuteEx` / OLE handoff |

### 8.2 D → C（桌面图标 → 集合）

| # | 子场景 | 触发链 | 提示文字 | 执行链 |
|---|---|---|---|---|
| D→C-1 | 空 slot / icon 外 | Slot → `SortDropZone::GetHint()` | "加入集合「xx」" | `SortDropZone::Execute()` → `Collection::OnItemsDropped()` → `MoveSelectedItemsToCollection` |
| D→C-2 | icon 上 | Slot → `HandoffDropZone::GetHint()` | "交给「xx」处理" | `HandoffDropZone::Execute()` → ShellExecuteEx |

### 8.3 D → F（桌面图标 → 桌面文件组件）

| # | 子场景 | 触发链 | 提示文字 | 执行链 |
|---|---|---|---|---|
| D→F-1 | 空 slot / icon 外 | Slot → `SortDropZone::GetHint()` | "加入桌面文件「xx」" | `SortDropZone::Execute()` → `FileCategories::OnItemsDropped()` → `AddToFileCategory` + `Reorder` |
| D→F-2 | icon 上 | Slot → `HandoffDropZone::GetHint()` | "交给「xx」处理" | `HandoffDropZone::Execute()` → ShellExecuteEx |

### 8.4 D → M（桌面图标 → 映射文件夹）

| # | 子场景 | 触发链 | 提示文字 | 执行链 |
|---|---|---|---|---|
| D→M-1 | 空 slot / icon 外 | Slot → `SortDropZone::GetHint()` | "复制到映射文件夹「xx」" | `SortDropZone::Execute()` → `FolderMapping::OnItemsDropped()` → `CopyFilesToFolder` + `Refresh` |
| D→M-2 | icon 上 | Slot → `HandoffDropZone::GetHint()` | "交给「xx」处理" | `HandoffDropZone::Execute()` → ShellExecuteEx |

### 8.5 D → E（桌面图标 → 外部）

| # | 触发链 | 提示 | 执行链 |
|---|---|---|---|
| D→E-1 | `DesktopDragSource::CreateDataObject()` → OLE `DoDragDrop` | 系统 | 系统 |

### 8.6 M → D（映射文件夹条目 → 桌面）

| # | 子场景 | 触发链 | 提示文字 | 执行链 |
|---|---|---|---|---|
| M→D-1 | 空 cell | Slot(空) → `EmptyDropZone::GetHint()` | "移动到此空位" | `EmptyDropZone::Execute()` → `DesktopGrid::OnItemsDropped()` → 文件复制到桌面 + MoveToCell |
| M→D-2 | 有图标，icon 外 | Slot → `SortDropZone::GetHint()` | 同 8.6-1 | `SortDropZone::Execute()` → DesktopGrid → 复制+就近放置 |
| M→D-3 | 有图标，icon 上 | Slot → `HandoffDropZone::GetHint()` | "交给「xx」处理" | `HandoffDropZone::Execute()` → ShellExecuteEx |

### 8.7 C/F → D（集合/桌面文件条目 → 桌面）

| # | 子场景 | 触发链 | 提示文字 | 执行链 |
|---|---|---|---|---|
| C/F→D-1 | 空 cell | Slot(空) → `EmptyDropZone::GetHint()` | "移动到此空位" | `EmptyDropZone::Execute()` → `DesktopGrid::OnItemsDropped()` → 从集合移除 + MoveToCell |
| C/F→D-2 | icon 外 | Slot → `SortDropZone::GetHint()` | "该位置已被占用" | `SortDropZone::Execute()` → DesktopGrid → 就近 |
| C/F→D-3 | icon 上 | Slot → `HandoffDropZone::GetHint()` | "交给「xx」处理" | `HandoffDropZone::Execute()` → ShellExecuteEx |

### 8.8 M → 同M（映射文件夹内排序）

**不涉及文件操作**，纯内存重排。

| # | 子场景 | 触发链 | 提示文字 | 执行链 |
|---|---|---|---|---|
| M→M同-1 | icon 外 | `SortDropZone::GetHint()` | "重新排序" | `SortDropZone::Execute()` → `FolderMapping::OnItemsDropped()` 同源检测 → `FinishWidgetMemberReorder` |
| M→M同-2 | icon 上(未选中) | `HandoffDropZone::GetHint()` | "交给「xx」处理" | `HandoffDropZone::Execute()` → ShellExecuteEx |
| M→M同-3 | icon 上(已选中) | `HandoffDropZone` 的 bounds 与选中 Item 重合 → `DragOp` 不将其设为 currentZone | "重新排序" | 走 SortDropZone 逻辑 |

### 8.9 M → 异M（跨映射文件夹）

| # | 触发链 | 提示文字 | 执行链 |
|---|---|---|---|
| M→M异-1 | `SortDropZone::Execute()` → `FolderMapping::OnItemsDropped()` 异源检测 | 同盘"移动"/异盘"复制" | `CopyFilesToFolder` + `Refresh` + `Reorder` |
| M→M异-2 | `HandoffDropZone::Execute()` | "交给「xx」处理" | ShellExecuteEx |

### 8.10 M → C/F（映射文件夹 → 集合/桌面文件）

| # | 触发链 | 提示文字 | 执行链 |
|---|---|---|---|
| M→C-1 | `SortDropZone::Execute()` → `Collection::OnItemsDropped()` | "加入集合「xx」" | 复制到桌面 → Reload → `MoveSelectedItemsToCollection` |
| M→F-1 | `SortDropZone::Execute()` → `FileCategories::OnItemsDropped()` | "加入桌面文件「xx」" | 复制到桌面 → Reload → `AddToFileCategory` + `Reorder` |

### 8.11 C/F → 同C/F（组件内排序）

| # | 触发链 | 提示文字 | 执行链 |
|---|---|---|---|
| C→C-1 | `SortDropZone::Execute()` → `Collection::OnItemsDropped()` 同源 | "重新排序" | `MoveSelectedItemsToCollection`(same idx) |
| F→F-1 | `SortDropZone::Execute()` → `FileCategories::OnItemsDropped()` 同源 | "重新排序" | `ReorderFileCategoryWidget` |

### 8.12 C/F → 异C/F（跨组件转移）

| # | 触发链 | 提示文字 | 执行链 |
|---|---|---|---|
| C→F | `SortDropZone::Execute()` → `FileCategories::OnItemsDropped()` | "移动到桌面文件「xx」" | 从集合移除 → `AddToFileCategory` |
| F→C | `SortDropZone::Execute()` → `Collection::OnItemsDropped()` | "移动到集合「xx」" | 从桌面文件移除 → `MoveSelectedItemsToCollection` |

### 8.13 C/F → M（组件 → 映射文件夹）

| # | 触发链 | 提示文字 | 执行链 |
|---|---|---|---|
| C/F→M-1 | `SortDropZone::Execute()` → `FolderMapping::OnItemsDropped()` | 同盘"移动"/异盘"复制" | `CopyFilesToFolder` + `Refresh` + `Reorder` |

### 8.14 E → D/W（外部拖入）

| # | 触发链 | 提示 | 执行链 |
|---|---|---|---|
| E→D-1 | `IDropTarget::Drop` → 解析 HDROP → 构建临时 `ExternalDropSource` | 同盘移动/异盘复制 | `DesktopGrid::OnItemsDropped()` |
| E→M-1 | 同上 → | 同盘移动/异盘复制 | `FolderMapping::OnItemsDropped()` |
| E→C/F-1 | 同上 → | 复制+加入 | `Collection/FileCategories::OnItemsDropped()` |

### 8.15 组件整体拖拽（Widget as Item）

| # | 触发链 | 提示 | 执行 |
|---|---|---|---|
| W-1 | 点击 Widget 标题栏 → Widget 选中 → 拖拽 | "移动组件" | `PlaceWidgetWithDisplacement`（不改，沿用现有） |
| W-2 | 拖到已占位 | "此处已被占用" | 不放置 |
| W-3 | 拖边缘 | "调整组件大小" | ResizeWidget（沿用现有） |

### 8.16 修饰键行为

所有 `Container::OnItemsDropped()` 和 `HandoffDropZone::Execute()` 接收 `int mods` 参数。文件操作时统一：

| mods | Disk operation |
|---|---|
| 0（默认） | `CopyFileEx` with MOVEFLAG if same drive |
| MK_CONTROL | `CopyFileEx` always copy |
| MK_ALT | `IShellLink` → `IPersistFile::Save` |

### 8.17 命中优先级

`DragOperation::OnMove()` 遍历 DropZone 的顺序：
1. 优先 `HandoffDropZone`（范围小，图标 rect）
2. 其次 `SortDropZone` / `EmptyDropZone`（范围大，整 slot）
3. 同一点优先返回 Handoff → 用户指向图标时触发 handoff，指向空白时触发排序

### 8.18 类方法对照速查

| 场景组合 | 涉及的类→方法 |
|---|---|
| 所有空 cell drop | `EmptyDropZone::Execute` → `Container子类::OnItemsDropped` |
| 所有排序 drop | `SortDropZone::Execute` → `Container子类::OnItemsDropped`（同源重排 or 异源插入） |
| 所有 handoff | `HandoffDropZone::Execute` → `ShellExecuteEx` 打开/处理 |
| 所有跨组件文件复制 | 目标 `Container::OnItemsDropped` 内调用 `CopyFilesToFolder` |
| 所有组件内重排 | 目标 `Container::OnItemsDropped` 内调用 `Reorder*` |
| 外部拖入 | `IDropTarget::Drop` → 构建临时 `ExternalDragSource` → `DragOperation::OnDrop` → 同上分发 |

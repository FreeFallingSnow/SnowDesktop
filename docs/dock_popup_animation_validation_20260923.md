# 文件类 Dock 弹窗动画连续性尝试

## 原始场景

- 用户在 `31f51957` 之后的开发版继续反馈“弹窗掉帧，感觉缺失中间帧”。失败反馈及宿主身份已记录在 `40372e51`，不能用上一轮自动测试通过代替实际画面验收。
- 2026-09-23 09:25:10 只读核对：运行 PID 9064，路径为 `.build/Release/SnowDesktop.exe`，SHA-256 为 `e9f0332383c8fa6013c8328ecc6aad910f4eb6d75f5d5471be9ea0abcc7260ae`。
- 原始日志中，工作区弹窗于 09:24:22.730 显示，09:24:22.747 返回 13 个目录条目，相隔 17 ms。此前多次工作区、下载和快捷访问的目录结果也在窗口显示后数毫秒返回。
- 代码确认：上一版内容刷新会准备实时画面，调用 `ResetCollectionPopupAnimationCache` 撤掉快照并取消完成回调，随后用 UI 帧推进余下动画。日志能证明数据在打开过程中到达，不能单独证明 DWM 实际丢了多少帧。

## 本次调整与依赖范围

- 内容刷新先区分合成器与 UI 动画。正在播放的合成动画只标记内容更新，不提前推进逻辑时钟、不取消完成回调、不改用 UI 帧。
- 同一批目录/图标结果合并到弹窗绘制入口。在宿主 Surface 的 `BeginDraw` 之前重新录制画面，并更新原有动画 Surface 的像素，保留缩放、透明度动画及 Visual。
- 播放中固定快照的几何和视图，防止数据到达时窗口与背景反复重排；原完成回调按最新内容计算实际尺寸，再准备实时画面并撤下快照。关闭、切换和设备清理仍清除待更新标记。
- 新增动画开始、录制耗时、合成内容刷新及完成回调耗时日志，用于后续定位；这些耗时不是 DWM 帧率测量。
- 改动涉及文件弹窗刷新、共享弹窗绘制入口和动画 Surface 像素复制。像素复制方法也被 Lua/快捷导航初次快照准备复用，因此除定向测试外，本候选需要一次完整自动回归。没有修改组件公共 API、用户数据格式、动画时长或图标工作队列。

## 回归边界

- `popup_animation_rules` 使用真实生产状态机和内容刷新分派，以回调替代 GPU 边界。检查打开时多次到达、迟于计划终点到达、关闭时到达均不接管原生时钟；完成仍能到达原终点。保留 UI 回退下先准备画面再撤缓存的检查。
- `dock_and_window_rules` 运行已有窗口/背景集成测试，并增加关键负向源码边界：像素更新不能重建 Visual/Surface、改缩放或透明度、等待 GPU 完成；内容刷新不能取消/重启动画或切回 UI 驱动。源码边界不作为实际 GPU 画面验证。
- 定向范围包括上述两项、`ui_animation_scheduler` 和 `widget_composition_layer_rules`，覆盖完成调度和共享图层生命周期；其余传递影响由完整自动回归检查。
- 隔离副本负向对照恢复“内容到达取消原生动画”、插入提前推进原生逻辑时钟、恢复先撤快照后准备画面，要求均成功编译并由业务断言失败。另对 Surface 更新边界插入取消动画调用，要求被源码边界拒绝。

## 验证状态

- 首轮标准 `scripts\build.bat --reload-shell`：退出 0，475.203 秒，Release EXE 成功生成，编译/链接无警告。EXE SHA-256 为 `67e52155a69a4a50bb7ec9491cf4524e393293418723fe4fdb8807df98cc205e`。
- 首轮 `scripts\test.bat name "^(popup_animation_rules|dock_and_window_rules|ui_animation_scheduler|widget_composition_layer_rules)$"`：4/4 通过，退出 0，总耗时 25.531 秒，CTest 1.15 秒；测试的立即退出回调使 `RefreshContent` 实例化时产生 C4702 不可达代码警告，本轮尚未完成警告清理。
- 隔离状态机候选通过；取消原生动画、提前推进时钟、提前撤快照三个错误变体均成功编译并由对应业务断言失败。源码边界负向对照及完整回归待执行。
- 先独立保存本次已编译的宿主尝试，随后修正测试断言写法并重新验证；警告不作为可接受的最终交付状态。
- 本机原始证据目录为 `.codex-probes/dock-animation-frames-20260923/`；用户日志不纳入 Git。
- 桌面宿主视觉验收待用户在本轮最终开发 EXE 上完成，未使用桌面自动化、未启动宿主冒充验收。重点场景：工作区、下载、快捷访问首次及重复打开，加载中关闭、切换、快速反向，网格/列表及扇形。
- 尚无目录条目数记录或内容数量实际改变时，完成后仍可能调整窗口尺寸；扇形逐项运动原有 UI 驱动保持不变。内容 Surface 更新失败不取消原生动画，正常完成时绘制最新实时内容；设备丢失仍由现有恢复路径处理。这些失败及视觉边界尚待实机场景验证。

## 最终自动回归检查点

- 宿主尝试独立提交为 `2387509b76ed3384a6ca7197c074a7e3eb948ee5`。随后仅把测试的立即退出回调改为计数并在调用后断言，保留错误分支的失败信号；没有压低警告等级或抑制 C4702，也没有再改宿主实现。
- 同组定向回归重新执行：4/4 通过，退出 0，总耗时 7.735 秒，CTest 1.05 秒；相应测试重新编译，C4702 消失，无其他编译/链接警告。
- 使用 `/O2 /W4` 重新编译隔离状态机候选及三个错误变体：候选通过，三个变体分别因取消原生轨道、接管时钟、提前撤快照触发预期失败，均无编译/链接警告。两个独立源码副本分别插入 `ResetCollectionPopupAnimationCache` 和 `SetScaleX`，边界检查均明确拒绝；未编译这些源码边界副本，不把词法检查计作 GPU 运行验收。
- 最终标准 `scripts\build.bat`：退出 0，31.062 秒，无编译/链接警告，EXE SHA-256 为 `f4e0c63220f6deeed2fa07d9b4cd2243a2ba3ff885b9795e50fbdad959c27543`。
- `scripts\test.bat full`：120/120 通过，退出 0，总耗时 131.547 秒，CTest 106.06 秒。默认排除 manual 项，没有执行手动 Shell 文件操作诊断；最终完整日志无编译/链接警告。
- 配置为 Release，Visual Studio 18 2026 / MSBuild `18.5.4+cb4e32d21`，Windows SDK `10.0.26100.0`。本轮 1093 项源码、测试、脚本、资源及构建输入的清单 SHA-256 为 `d248a1c586293d2c603c7c9ebd4b687edb8003af0b18a73e35de2b6e85b8224b`；回归前后无变化，与首轮标准构建清单相比只有上述测试源码变化。
- 全量测试再次链接后的最终开发 EXE SHA-256 为 `ec3eee44a54b2d7e427eedb2d632c5342424b2d32c126722e166f7a8481e19e7`。命令、起止时间、退出码、完整日志哈希、输入和负向对照均见本机证据目录的 `validation.json`。
- 自动回归通过不关闭原始缺帧反馈；该 EXE 的桌面动画实机验收仍待用户完成。

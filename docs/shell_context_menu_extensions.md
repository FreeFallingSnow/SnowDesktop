# 系统右键菜单同步与二次隐藏

设置 → 右键菜单保留菜单样式选择，并以“文件 / 文件夹 / 文件夹空白处 / 桌面”
四个场景展示系统实际返回的菜单。每行只有原始图标、名称和显示开关。
优先使用系统提供的图标；缺图且名称精确匹配扩展注册项时，只读加载原模块的小图标资源，
仍无资源时设置列表使用通用图标。系统已禁用或当前对象不适用的项目不会被自行加入。

默认跟随系统，不再要求手动勾选扩展组或指定放置位置。原始菜单项直接接入自绘
右键菜单，保留第三方自身的级联关系；例如根菜单直接显示“7-Zip”，不增加
“系统扩展”和“7-Zip Shell Extension”包装层。第三方项目位于“展开更多选项”
上方，与它同组，中间不额外插入分隔线。宿主已有的文件操作与桌面操作
继续由宿主处理，避免重复菜单和绕过应用内文件操作语义。

## 设置与对象范围

开关仅追加或移除 SnowDesktop 的隐藏记录，不修改系统注册表，也不启用系统已禁用的扩展。
四个场景独立保存。菜单调用时重新取得实际对象的系统菜单，再应用相应隐藏记录。
混合选择文件和文件夹时，两类中的隐藏记录均有效。

设置页使用隔离的普通文本文件/文件夹读取默认列表，桌面页读取真实桌面背景菜单。
无需先选样本；涉及特定文件类型、版本库或网盘的项目，可通过页底的可选入口查看
具体文件/文件夹。四类默认列表缓存 30 秒，切回已有分类可直接显示；刷新会绕过缓存。
具体对象每次重新查询，查看过程不执行任何菜单命令。

隐藏项优先使用 canonical verb 标识。没有唯一 canonical verb 时使用名称和子菜单
命令签名；这类项目更改语言、名称或命令结构后可能需要重新隐藏。旧版实验选择器的
enabled / selections 字段为兼容回存而保留，运行时不再用它们控制启用和嵌套位置；
新的 hidden 数组保存实际生效的场景隐藏记录。

## 系统查询与进程边界

--shell-menu-helper 是内部同源子进程模式，不是公共 CLI。组件公共 API 不变。
查询从实际 Shell 文件选择或背景的 IContextMenu 聚合对象获得菜单，不再逐个
实例化注册表中的处理器。系统可见性、LegacyDisable、扩展当前对象条件由 Shell
决定；本地设置只能继续减少项目，不能恢复 Shell 未提供的项目。

菜单、COM 对象、站点和命令映射留在同一子进程中，直到关闭菜单或执行选中命令。
图标同时读取 MIIM_BITMAP 和 MIIM_CHECKMARKS（包括 7-Zip 使用的 hbmpUnchecked），
并支持 Shell 回调位图。7-Zip 默认关闭菜单位图时，补充读取其原模块的 13x13 资源，
不修改 7-Zip 设置。资源模块以 DATAFILE 加载，不执行 DLL 初始化。
图标以有界像素传输，宿主和设置页不接收第三方原生句柄。
首次文件夹查询前用内核自动删除的隔离文本样本初始化文件关联，以避开本机观察到的
扩展冷启动加载锁；不显示或执行样本菜单。查询仍在同一个有期限的辅助进程内。
查询超过 8 秒终止辅助进程，关闭菜单或切换页面取消会话。级联打开期间仍处理超时，
返回数据待级联关闭后再合入，并保留键盘高亮及屏幕边缘约束。

调用保留原会话、真实命令和当前可用状态。辅助进程有 120 秒上限，允许交接至独立
应用和显示对话框；特殊自绘项目及延迟加载的级联可回退对应原生菜单，查询时不强制
填充所有子菜单。原有“更多选项”沿用已有路径，
不能据此宣称所有 Shell 调用均已隔离。

## 查询缓存与键盘访问键

每个 UI 线程最多保留一个成功查询且未执行命令的空闲辅助进程，最长复用两分钟。
菜单关闭后异步释放原生菜单与 COM 对象，确认释放后删除该会话的隔离样本；后续查询
可以提前排队，但释放确认不会清理新会话的样本。复用时为当前对象重新查询并建立命令映射；不会按文件类型
套用另一文件的菜单，也不把旧命令编号用于新对象。已取消、失败、超时或开始执行的
会话不进入空闲池，同时打开的会话各自拥有进程和菜单对象。

辅助进程缓存按场景查到的图标注册元数据，以及最多 64 个扩展模块的原始位图资源。
模块大小或修改时间变化后重新读取资源。父进程以注册表事件监听文件关联、扩展、
资源管理器策略和应用主题变化，下一次查询丢弃旧进程；手动刷新也会失效缓存。
查询本身继续由真实 Shell 决定项目、禁用状态和命令，首次冷启动仍需加载第三方扩展。

设置列表缓存仅保存显示数据，递归清除所有命令令牌；四个默认场景独立，过期、注册项
变化或刷新后重新读取，不缓存用户指定对象的菜单。缓存过程不修改第三方注册项。

原生文字中的 `&` 访问键标记在解码时单独保留，`&&` 保持字面 `&`，不将文件名中的
普通括号或右侧快捷键列猜作访问键。菜单中的唯一字母键立即执行或展开子菜单，重复
访问键循环选中并由回车确认，跳过禁用项目，支持 Alt 字母和大小写匹配。搜索框有焦点时
普通文字仍输入搜索框。原有方向键与无显式访问键的首字母选择行为保留。
按键在消息转换前匹配，避免重复字符动作；输入法产生 `VK_PROCESSKEY` 时使用
[ImmGetVirtualKey](https://learn.microsoft.com/en-us/windows/win32/api/imm/nf-imm-immgetvirtualkey)
取得原始虚拟键。中文输入法的桌面实机效果仍待用户验证。

## 缓存与访问键候选（2026-09-19，f567ac90）

本轮用现有 Shell 测试程序的 `--benchmark-shell-menu` 按需入口，针对隔离文本文件和
文件夹连续立即查询四次，不显示或执行第三方命令。基线为 `3207cd94`，
最终代码为 `f567ac90`；MSVC、Release 配置和本机扩展环境一致。

| 样本 | 基线每次重建进程 | 最终候选的后续三次查询 |
| --- | --- | --- |
| 文本文件 | 1016–1125 ms | 438 / 500 / 703 ms |
| 文件夹 | 1344–1703 ms | 234 / 203 / 204 ms |

最终首个冷文件查询为 1047 ms，首次文件夹查询为 531 ms，八次查询复用同一进程。
连续立即查询会包含上一菜单异步释放的等待；中间保留旧菜单对象的更快测量不作为最终结果。
以上为本机查询基准，不代表桌面端到端延迟或所有扩展的性能保证。
日志为 `.codex-probes/shell-menu-cache/baseline-benchmark.log` 和 `final-benchmark.log`。

定向回归检查进程复用后读取新对象、并发会话独立、失败/超时/取消恢复、注册禁用导致
失效、设置缓存过期及显示数据剥离命令令牌，以及旧样本释放确认与新查询交错。
访问键回归在独立弹窗中先确定复现“按键后返回取消”，再以同一输入验证目标命令，
并覆盖大小写、Alt、重复访问键、禁用项、级联、搜索输入和消息转换前的按键处理。
最后定向命令为 `scripts/test.bat name "^(shell_context_menu_invoke|modern_menu_interaction)$"`，
2/2 通过，退出码 0；日志为 `.codex-probes/shell-menu-cache/final-targeted.log`。

缓存与访问键代码 `f567ac90` 的完整自动集合已通过：`scripts/test.bat`，
118/118，退出码 0，CTest 用时 86.34 秒；报告为
`.build/Testing/test-run-216d378dbf5e48bdbcb9a560ade0d696.xml`，日志为
`.codex-probes/shell-menu-cache/full-final.log`。桌面交互仍待实机；后续共享磁盘缓存
将改变相关运行输入，不能直接沿用该全量结论作为新候选的最终验证。

## 参考与验证边界

交互参考火绒的分类开关列表，以及
[ContextMenuManager](https://github.com/BluePointLilac/ContextMenuManager) 的按场景管理；
直接处理原有菜单项的思路参考
[Nilesoft Shell](https://nilesoft.org/docs/configuration/modify-items)。
7-Zip 图标兼容根据其
[公开实现](https://github.com/ip7z/7zip/blob/main/CPP/7zip/UI/Explorer/ContextMenu.cpp)
及[默认图标选项](https://github.com/ip7z/7zip/blob/main/CPP/7zip/UI/Common/ZipRegistry.cpp)
核对，未复制项目代码。

自动化使用隔离目录和独立菜单窗口；真实 7-Zip 查询不执行其命令。系统禁用回归只创建
唯一的临时 HKCU 文件类型/静态命令，测试 LegacyDisable 的真实 Shell 行为后清除，
不修改现有文件关联、第三方注册项或用户文件。桌面宿主不通过自动化操作。
首版仍以经典 Shell 菜单为范围，Windows 11 仅新式注册项不保证提供。
设置列表视觉、实际桌面/Dock/文件夹映射菜单及第三方操作需用户实机验收。

## 前轮界面候选验证（2026-09-19，55d2155e）

分支 `release/v1.0.7.0`，版本保持 `1.0.7.0`。运行输入对应 `55d2155e`，
之后仅将 Shell 测试局部变量 `archive` 改名为 `archiveEntry` 清理重名警告，
未改变生产代码、测试条件、资源或宿主构建输入；该条目已重新编译并单独复测。

| 本次执行 | 结果与范围 |
| --- | --- |
| 菜单、设置、配置、导航与本地化定向测试 | 6/6 通过，退出码 0 |
| `scripts/test.bat` | 自动集合 118/118 通过，退出码 0；配置 2.07 秒，编译 45.06 秒，测试 92.74 秒 |
| `scripts/test.bat name "^shell_context_menu_invoke$"` | 清理测试警告后 1/1 通过，退出码 0，测试 8.55 秒 |
| `scripts/build.bat` | 标准 Release 构建通过，退出码 0，总计 34.31 秒；预检无占用，无需重载 Shell |

全量报告为 `.build/Testing/test-run-d06485af99eb4dcaa469152a0bcd5623.xml`，
最后定向报告为 `.build/Testing/test-run-4d0e47f7971149978ca75b8495f57d06.xml`。
本地详细日志及时间记录位于 `.codex-probes/shell-extension-redesign/` 的
`full-final.log`、`targeted-warning-cleanup.log`、`build-final.log` 和对应 `*-result.json`。
全量日志中的 WinUI `GetCurrentTime` C4002 是已有警告；测试 C4456 已清理，
最后定向编译和标准增量构建均未输出新增警告。

环境为 MSVC `19.50.35730.0`、Windows SDK `10.0.26100.0`、CMake `4.3.1`。
本机四类实际系统菜单查询通过，文件和文件夹的 7-Zip 根项目各返回 `13 × 13`、
`676` 字节图标数据；未执行第三方命令。相同输入此前因空图标失败，
`bea3bc29` 调整 GDI 句柄判断后通过，形成实际负向对照。
已有菜单数据、开关场景隔离、系统禁用、无额外包装和“更多选项”上方同组位置均有回归覆盖。

交付产物 `.build/Release/SnowDesktop.exe` 的 SHA-256：
`872C629B3F26C8B6F9563F65201ADBF52A13262A1946510D6C29F938CE727F78`。
手动诊断条目未运行。设置页浅深色与缩放显示、桌面/Dock/映射目录的真实右键交互、
7-Zip 等第三方命令执行及系统变更后的刷新仍待用户实机验收；本轮仅作为 `try` 候选交付。

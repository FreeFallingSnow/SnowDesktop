# 大图标逐项完成审计

## 当前 v2 重构状态（2026-09-08）

当前验收范围以 [第二版规划](large_desktop_icons_settings_v2_plan.md) 及用户后续反馈为准。下方旧轮次的 117 项结果只属于当时版本，不证明 v2 重构通过；其中浮动标题、预览和旧背景组织已被新规划替代。

- 当前标准 `scripts/build.bat` 通过；首轮矩阵初始化调用编译失败后已调整并重新通过。生成 WinUI 头的 C4002 为既有警告。
- `dddfa410` 首轮定向检查 5/6 通过；唯一失败来自短标题居中后的半像素边缘，64 像素原图包含 63 列实心像素及两列半透明像素。调整为按透明度加权核对尺寸后，`scripts/test.bat full` 117/117 通过（72.02 秒），隔离输出检查通过。
- 当前生产离屏检查覆盖标题字重/颜色、实际文字宽高对应的位移、两行省略、填充透明裁切、实际 WARP 整框 3D 与透明度；已导出并目检短标题、长标题和整框 3D 图。完整测试同时覆盖设置 IPC、背景互斥、缩放裁切余量、自动方向、旧配置兼容和全部语言文案。
- 新增填充缩放、自动标题方向、实际文字测量和更快过渡；图标背景色改用美化底色规则，与主题色渐变互斥。旧 v2 配置保留手动方向并归一化旧嵌套渐变；全部十种语言同步。
- 原生设置窗口排布、背景与图标衔接、自动方向和位移观感、3D 原生模糊对齐仍待用户实机验证，不能报告完整规划已验收。

## 旧轮次记录

### 新增三种效果与移交提示的待验证场景

- 右键及详细设置可选轻微缩放、边缘柔光、单次扫光；菜单连续切换保持打开，悬停显示/隐藏桌面保留两项不会与新效果串用。锁定时不能修改，已保存效果继续显示。
- 新建普通大图标默认动态标题，新建 Steam 填充默认轻微缩放；效果重置按当前背景模式选择默认值。动态标题切入填充后改用轻微缩放，其余可用效果保留；重启加载旧配置不自动改变已有效果。
- 三项只披露当前参数，滑杆、数值和重置一致；切换模式及重启后保留各自参数。原始图、替换图及 Steam 填充均可用，填充仍不支持动态标题。
- 快速移入移出缩放、柔光连续；扫光只播放一次，停留不循环。全局关闭动画后无缩放和扫光、柔光直接显示；页面不可见、拖动、调整尺寸后无残留或持续刷新。
- 透明 PNG、非居中裁切、完整显示留白、100% 圆角和多 DPI 下，扫光不涂满透明区域，缩放不改变外框。柔光观感与三项默认强度由用户实机确认。
- 从桌面及外部拖文件到一个 3×2 大图标，在起始格及其他覆盖格观察“交给 xx 处理”：绿色框均覆盖完整外框，并跟随独立/组件圆角。拖拽源虚影仍为单格；普通图标及 Dock 目标提示保持原大小。

该轮实机项目尚未验收；构建和自动化结果以对应 `try` 提交记录为准。

依据用户确认的完整规划核对，不以当前已实现范围重定义验收。2026-09-08 起持续更新。代码存在、自动化通过、真实场景通过是不同证据，不能互相替代。当前功能代码、标准构建和 117 项自动化检查已完成；宿主实机验收尚未完成。

用户 2026-09-08 提供的设置窗口截图确认 `65a0c25b` 所在版本存在左侧设置窄列、右侧大量空白，设置布局验收未通过。本轮改为横向拉伸的卡片与现有自适应设置行，并根据反馈调整组件式缩放手柄、支持中键拖拽、补齐右键命令图标、取消转换后自动打开设置。以下新增行为已编译并通过自动化检查，尚待新版实机复测。

用户随后反馈大图标左上角有小圆圈并要求移除。已删除额外选中角标、保留独立描边；生产 Direct2D 像素检查及导出图目检通过。该反馈未确认其他五项全部通过，仍保留其复测状态。

最新分级设置与拖动调整已完成标准构建和 `scripts/test.bat full`（117/117，49.11 秒，隔离输出检查通过）。生产离屏图确认右侧大字居中、无衬底，框内仍可两行省略；自动化同时覆盖百分比圆角兼容、设置适用性、私有默认值及尺寸传输、单格虚影抓取点和固定层级。首次完整测试的圆角说明占位符误识别已通过全语言文案修订消除。该证据不代替 WinUI 控件排布、拖动闪烁、尺寸停绘和手柄指针的实机验收。

## A：状态、权限与保存

| 要求 | 当前证据 | 尚需完成或取得的证据 |
| --- | --- | --- |
| 原项目身份、打开目标、Shell 行为保留 | `DesktopItem` 增加可选配置；`DesktopIcon::Draw` 分流展示，原操作路径保留 | 实际打开、重命名、属性、菜单、拖给应用 |
| 创建、外观、尺寸、导入、设置打开、主动刷新由宿主校验 | 宿主入口检查现有 `IsRegistered()`；共享写入与会话规则测试覆盖有效/失效、创建/修改/还原、容器拒绝、旧会话和旧修订 | 有效、离线有效、失效、重新解锁及编辑中失效的状态场景验证 |
| 失效保留存量和正常使用，专属配置只读 | 授权通知撤销预览；自动资源完成不依赖编辑资格；还原空配置不要求解锁 | 实机切换资格并核对状态和动效不变 |
| 已有布局和备份恢复允许保留；复制和模板不能绕过创建检查 | 可选配置随布局解析，无配置不由跨度推断；普通 Shell 复制按新身份生成 | 已核对：目前无桌面模板或实例复制入口；`FileGroup::CloneHostedItem` 只创建同一项目的视图。实际 Shell 文件复制仍需实机确认 |
| 私有编辑会话含身份、版本、修订号；删除、还原、迁入容器失效 | 私有 IPC 编解码测试；宿主状态检查和配置清除路径 | 旧会话提交、编辑中重命名及进程退出重开验证 |
| 保存由宿主完成，失败回滚并能重试 | 生产 `large_icon_edit_rules::Store` 的失败、异常、重试测试通过；原配置与跨度回滚，宿主恢复布局记录；设置页保留重试按钮 | 设置窗口错误显示、连续编辑取消与重试的实机验证 |
| 首次新格式写入前独立完整备份 | 生产 `EnsureLargeIconUpgradeBackup` 测试通过：首次完整备份、后续保存保留旧快照、目标目录受阻返回失败且不改活跃布局 | 用户数据上的首次创建和升级回退操作核对 |
| 布局根版本不变、可选对象自带版本、数据有限值校验 | `large_icon_config.h` 和布局往返/非法值测试 | 完整备份旧版回退、重新升级恢复实测 |
| 备份带手选图和当前封面，排除授权缓存 | `application_data_lifecycle` 完整备份与素材文件保留测试 | 用户实际数据恢复后的展示核对 |

## B：展示与输入

| 要求 | 当前证据 | 尚需完成或取得的证据 |
| --- | --- | --- |
| 同跨度与组件外框、间距一致，不减标题行 | 使用 `GetStandaloneWidgetFrameRect` 和相同网格矩形 | 同页同跨度、多 DPI 对照 |
| 默认原始图居中 60%，不放大低清图，不走全局美化 | 独立 Shell 原图服务及缓存；绘制分支在普通美化之前；生产离屏像素确认低清原图不放大、默认悬停不移位 | 真实透明图、不同原图尺寸、打开全局美化的视觉对照 |
| 立即有背景，自动取色忽略透明，失败使用中性色 | 生产离屏背景检查及全透明/低透明 PNG 解码测试通过；无取色结果时随当前主题使用中性色 | 半透明、全透明和取色失败样例 |
| 常态/悬停背景、手动色、混合比例、透明选择分别保留 | 配置编解码与独立设置控件 | 所有背景组合视觉验收 |
| 圆角裁剪、图片、边框共用几何；选中标记独立 | 生产 Direct2D 离屏像素检查和目检覆盖同圆角裁剪、独立选中描边、DPI 阴影；额外角标已移除，回归检查圆点消失且透明背景描边仍存在 | 新版透明背景、圆角边缘、边框和阴影实测 |
| 默认浮动标题不移动原图、不占固定行、不抢点击 | 独立绘制层，无命中对象；生产离屏图确认下方标题、屏幕底部上移及长名称展示 | 长标题、屏幕边缘、键盘聚焦、透明区域点击 |
| 整个矩形可选择、打开、菜单、拖动 | 大图标命中采用外框，保留原点击路径 | 混合框选、单击/双击打开次数和透明角落 |
| 拖动、缩放、菜单期间隐藏标题并暂停悬停 | 输入条件和标题绘制前置判断 | 快速交错操作无残留状态 |
| 中键拖动大图标，中键单击不启动 | 复用桌面拖动阈值、选择和取消路径；浮动表面、OLE 和返回桌面均判断发起按键；规则测试覆盖中键持续、释放及原生交回 | 中键单击、混合选择拖动、Dock/集合接收、外部窗口与返回桌面实测 |
| 新增右键命令有图标，转换后不自动打开设置 | 创建/设置/还原均指定菜单图标；直接创建及占位预览确认均取消自动打开设置，未解锁仍进入解锁页 | 真实 Shell 菜单展示、两种创建路径与未解锁入口复测 |

## C：创建、跨度与容器

| 要求 | 当前证据 | 尚需完成或取得的证据 |
| --- | --- | --- |
| 2×2 原位置尝试；空间不足占位预览；合法任意跨度；Esc 不修改 | 创建检查、手柄、键盘占格、确认/取消路径 | 满页、1×1 页、相邻组件、取消及保存失败 |
| 缩放手柄与组件一致 | 复用无标题组件悬停显示规则和命中矩形，绘制组件同尺寸圆角点，按网格缩放 | 相同页面、相同缩放下对照组件手柄外观及拖动命中 |
| 移动、排序、跨页、跨屏保留跨度，非法主动落点拒绝 | 大图标群组边界检测和按完整跨度放置；翻页预览不迁移源；桌面来源包含大图标时不复用邻近落点缓存、不自动找空位。生产搜索回归通过：占用、1×1、完整跨度越界、混合群组、跨页与普通图标邻近搜索 | 混合群组、名称/类型排序、多显示器拖动和取消 |
| 期望跨度与临时适配分开，显示环境恢复后重新尝试 | `LargeIconConfig` 与布局 `gridSpan` 分开；恢复尝试和碰撞安置 | 屏幕缩小、断开、重新接入、重启及冲突组合 |
| 收纳容器成功接收才退化；保留桌面源则保留原实例 | 容器成员关系提交后清除配置；Dock `keepOnDesktop` 排除源迁移 | Dock/悬浮 Dock/集合，拒收、取消、部分成功、保留源和拖回 |
| 应用打开文件与组件处理引用不退化 | 文件提交与收纳成员关系采用不同路径 | 真实应用文件打开及 Lua 引用处理对照 |

## D：详细设置与轻量效果

2026-09-08 后续反馈要求设置分级、控件右对齐、逐项重置、百分比圆角、较大且无衬底的右侧居中标题、不可用效果置灰；另要求单格拖动虚影、排查虚影与提示交替闪烁、缩放期间停绘，以及组件手柄指针统一。本轮代码已纳入这些调整，验证结果见本次提交；桌面输入和独立 WinUI 窗口仍需新版实测。

| 要求 | 当前证据 | 尚需完成或取得的证据 |
| --- | --- | --- |
| 现有 WinUI 设置进程，当前项目定位，三组设置，实时预览 | 私有路由、IPC、原生设置页及全部配置控件；设置容器横向拉伸、复用自适应设置行、顶部合并名称/来源/预览 | 用户截图已确认旧版窄列问题；新版宽窄窗口、滚动、多 DPI 和键盘操作待复测 |
| 原图/静态图片/Steam 内容、适配、焦点、标题、全部边框背景参数 | 配置字段清单与设置控件对应 | 预览和宿主最终显示的一致性 |
| 恢复原图保留大图标；还原普通图标始终可用 | 两个独立动作，修改与还原走不同授权条件 | 锁定状态及素材加载中执行还原 |
| 连续调整预览，结束提交；Esc 取消，失效取消未提交状态 | 滑块及颜色预览统一 Esc 取消；宿主会话规则测试覆盖失效撤销未提交预览 | 实机确认键盘、鼠标捕获、切换页面与窗口关闭时的提交/取消行为 |
| 左移显标题保留原尺寸，右侧大字居中无衬底，两行省略 | 共用内容几何和可用性规则；默认框内字号 24 CU；设置的左移选项按内容、尺寸和动画状态禁用 | 快速进出、长标题、宽窄框、封面、减少动画与设置预览对照 |
| 悬停主位移互斥，可叠加一种外框效果；按下和启动单次反馈 | 控件联动与宿主效果绘制 | 状态机已验证时序、反向连续性和启动只执行一次；还需实际鼠标/键盘触发核对 |
| 设置预览完整反映外观及动效 | 预览已共享完整动效状态及几何，加入阴影、启动和宿主动画/帧率参数 | 独立 WinUI 设置窗口的实际显示、滚动、DPI 和交互核对 |
| 服从全局动画、节能、帧率；静止与不可见不持续刷新 | 使用宿主动画开关与调度，结束取消刷新 | 状态机静止/隐藏不请求刷新已通过；整机 CPU/GPU、能耗及多图性能仍需实际测量 |

## E：素材获取与资源边界

| 要求 | 当前证据 | 尚需完成或取得的证据 |
| --- | --- | --- |
| Steam URL 两种协议，拒绝非商店长 ID | AppID 解析及边界测试 | 真实快捷方式与商店不存在的合法 ID |
| 自有缓存、本地旧平铺/新目录/哈希目录优先 | WIC 素材测试含三类目录；新版 `library_capsule` 已补齐 | 更多实际 Steam 客户端缓存样本 |
| 元数据真实路径、横竖、高分辨率、语言回退 | 替身测试覆盖语言回退及哈希路径；真实服务 integration 使用生产素材与 HTTP 源码，570/schinese、620/english、1091500/japanese 的横竖六项下载及 WIC 解码通过，共 3,069 ms | 宿主内真实加载、更多游戏、实际缺失语言回退 |
| 仅本地无新请求，允许已缓存图片；手选图优先 | 素材测试检查 HTTP 次数；真实服务重新构造后，六项仅本地请求在 86 ms 内全部读取缓存，尺寸及文件修改时间保持；宿主请求代次保护 | 实际断网、编辑中切换仅本地和选择自定义图的并发验证 |
| 源文件 16 MiB、1600 万像素、格式白名单、复制后使用 | 实际 WIC 解码和非法/超限文件测试，源字节保真 | 真实 BMP/JPEG 和多帧 ICO 样例、有效超像素 PNG 已通过；宿主中损坏缓存恢复与提示核对 |
| 两任务上限，相同 AppID/类型/语言请求合并 | 可控制网络边界的并发及去重测试；共享失败后按监听项目分别回退，旧封面与真实 Shell 原图隔离测试通过 | 宿主中的多实例并发显示验证 |
| 失败保留旧图，404 24 小时，短暂失败退避，刷新可重试 | 素材失败缓存与刷新测试；宿主保留当前位图 | 各类下载失败、取消和刷新交错回归 |
| 128 MiB 解码预算、引用保护、未引用缓存回收，后台不阻塞桌面 | 扫描和排序移出请求锁，删除前重新校验引用；小预算下的活跃图片保护、释放后恢复、自动磁盘回收测试通过 | 自动文件命名边界已测试，保留非管理文件和相似前缀文件；实际大容量与慢磁盘性能待测 |
| 原图与美化缓存隔离，按显示尺寸/DPI，资源变更重算取色 | 原图服务独立；新测试验证 Shell 索引及源时间变化失效 | 相同快捷方式同时位于大图标与 Dock 的视觉对照 |
| 迟到回调不覆盖手选、已还原/删除/迁出实例 | 取消和代次测试；宿主身份和容器检查；已入完成队列的缓存命中取消测试通过 | 与编辑会话切换的宿主运行验证 |

## F：交付门槛

- 每轮编译成功按双语 `try` 提交记录，再开始下一轮原生代码调整；不将自动化当作实机验证。
- 新增界面文案同步全部 10 种语言，运行本地化契约，并人工检查翻译。
- 最终运行 `scripts/build.bat`、`scripts/test.bat full`；针对新增集成场景使用 CTest `integration` 标签。
- 桌面视觉、悬停、框选、拖放、Dock 由用户实机验证或稳定的等价可观察证据证明，不能调用桌面自动化绕过仓库限制。
- 最新 `scripts/build.bat --reload-shell` 标准构建通过；本轮 `scripts/test.bat name icon_render` 为 3/3，耗时 1.12 秒；最近完整测试为 `2f4d91b7` 的 117/117、47.89 秒，隔离输出检查通过。本轮按连续视觉调校规则未重复完整测试。移除角标后的生产离屏图已导出并目检，不替代桌面实机复测。
- Shell 重载的现有 `timeout` 命令仍提示输入重定向不受支持，构建继续并成功退出。此前生成 WinUI 头的 C4002 警告未在本轮处理；本轮仅重编渲染文件。未将编译通过解释为实机验证。
- 上述实现缺口已补入代码；剩余实机及真实环境证据仍待取得，不能将当前状态标记为“全部规划已验收”。

## 首轮实机检查顺序

运行 `.build/Release/SnowDesktop.exe`，使用现有有效解锁，依次检查下列场景。这里的预期结果不是已取得的实机证据；结果由用户反馈后记录。

| 操作 | 预期结果 |
| --- | --- |
| 单选普通快捷方式，右键“转为大图标”；再尝试空间不足时的占位预览创建 | 创建命令有图标；有空间时使用 2×2；两种转换成功均留在桌面，不自动弹出设置 |
| 开启全局图标美化，对照普通图标与大图标 | 原图仍居中、背景立即可见，原图不随全局美化改变 |
| 从有图标的“大图标设置”进入，调整窗口宽度、滚动并试用颜色按钮 | 卡片铺满可用宽度；宽窗口标签和控件左右排列，窄窗口上下排列；预览、来源和名称集中，颜色按钮显示色块与值 |
| 对照同页组件的右下手柄，再改变大图标宽高，尝试 1×1 和横长框；拖动中按 Esc | 悬停显示同尺寸圆角点；合法调整松手保存，Esc 保留调整前状态；1×1 仍是大图标 |
| 选中大图标，分别使用默认背景、透明背景及封面 | 左上角无额外小圆圈，选中状态通过外框描边识别 |
| 中键单击大图标，再中键拖动到桌面、Dock、集合和外部窗口，尝试拖回及取消 | 中键单击不启动；移动超过阈值才拖动；按中键释放完成，取消保留源，容器退化规则与左键一致 |
| 在设置中选择“图标左移显示标题”，快速移入移出，再改成竖长框 | 横框原图保持尺寸左移，右侧大字居中且无衬底；反向过渡连续，竖框左移选项置灰，已有配置回退浮动标题 |
| 切换原图、本地图片、Steam、自动/手动颜色、边框、阴影和效果，并逐项重置 | 仅显示适用选项；动作和编辑控件靠右；每项重置恢复对应默认值；圆角 0–100%，重置与当前组件一致 |
| 拖动大图标到桌面、Dock、集合及弹窗附近，持续移动和停留 | 虚影在单格内等比缩小，抓取点稳定；提示始终在虚影上方，不交替闪烁；落点仍按原跨度判断 |
| 调整大图标大小，松手、Esc 取消；再悬停并拖动组件两种手柄 | 缩放期间只画占位框，结束恢复内容；缩放手柄显示斜向缩放指针，移动手柄显示移动指针，结束和取消无残留 |
| 拖到被占网格、页面末列和另一页的越界落点，再尝试合法落点 | 非法落点不移到附近空位、不修改源；合法落点保留完整跨度。混合选择也应如此 |
| 将 Steam `.url` 转换并切换横向/竖向、仅使用本地图片，再选择本地图片 | 显示实际素材及来源，缺图保留原图背景；手选图片不会被迟到的自动封面覆盖 |
| 分别关闭和开启 Dock“保留桌面项目”后拖入，再拖回桌面 | 实际迁入后为普通入口，拖回仍普通；保留源时桌面原实例保持大图标 |
| 关闭并重新打开设置、重启应用，再检查尺寸、图片和效果 | 已提交配置保持；“恢复原始图标”只换内容，“还原为普通图标”退出大图标 |

授权状态切换、备份回退、显示器断开恢复和性能验证仍按上方完整矩阵完成，不以这组初查替代。

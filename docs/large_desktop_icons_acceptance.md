# 大图标逐项完成审计

依据用户确认的完整规划核对，不以当前已实现范围重定义验收。2026-09-08 起持续更新。代码存在、自动化通过、真实场景通过是不同证据，不能互相替代。当前目标尚未全部验收。

## A：状态、权限与保存

| 要求 | 当前证据 | 尚需完成或取得的证据 |
| --- | --- | --- |
| 原项目身份、打开目标、Shell 行为保留 | `DesktopItem` 增加可选配置；`DesktopIcon::Draw` 分流展示，原操作路径保留 | 实际打开、重命名、属性、菜单、拖给应用 |
| 创建、外观、尺寸、导入、设置打开、主动刷新由宿主校验 | `SetLargeIconConfig`、`EditLargeIcon`、`OpenLargeIconSettings`、手柄入口检查现有 `IsRegistered()` | 有效、离线有效、失效、重新解锁及编辑中失效的状态场景验证 |
| 失效保留存量和正常使用，专属配置只读 | 授权通知撤销预览；自动资源完成不依赖编辑资格；还原空配置不要求解锁 | 实机切换资格并核对状态和动效不变 |
| 已有布局和备份恢复允许保留；复制和模板不能绕过创建检查 | 可选配置随布局解析，无配置不由跨度推断；普通 Shell 复制按新身份生成 | 逐条核对模板、内部实例复制入口，补足创建路径覆盖 |
| 私有编辑会话含身份、版本、修订号；删除、还原、迁入容器失效 | 私有 IPC 编解码测试；宿主状态检查和配置清除路径 | 旧会话提交、编辑中重命名及进程退出重开验证 |
| 保存由宿主完成，失败回滚并能重试 | 保存返回值及原配置、跨度、布局记录回滚；设置页重试按钮 | 保存失败注入与预览状态取消的行为测试 |
| 首次新格式写入前独立完整备份 | `SaveLayoutSlots` 在写布局前调用独立 `LargeIconUpgradeBackup` 完整备份 | 首次写入及备份失败阻止写入的实际验证 |
| 布局根版本不变、可选对象自带版本、数据有限值校验 | `large_icon_config.h` 和布局往返/非法值测试 | 完整备份旧版回退、重新升级恢复实测 |
| 备份带手选图和当前封面，排除授权缓存 | `application_data_lifecycle` 完整备份与素材文件保留测试 | 用户实际数据恢复后的展示核对 |

## B：展示与输入

| 要求 | 当前证据 | 尚需完成或取得的证据 |
| --- | --- | --- |
| 同跨度与组件外框、间距一致，不减标题行 | 使用 `GetStandaloneWidgetFrameRect` 和相同网格矩形 | 同页同跨度、多 DPI 对照 |
| 默认原始图居中 60%，不放大低清图，不走全局美化 | 独立 Shell 原图服务及缓存；绘制分支在普通美化之前；源尺寸规则测试 | 真实透明图、不同原图尺寸、打开全局美化的视觉对照 |
| 立即有背景，自动取色忽略透明，失败使用中性色 | 绘制在素材获取之前；取色像素过滤和中性色回退 | 半透明、全透明和取色失败样例 |
| 常态/悬停背景、手动色、混合比例、透明选择分别保留 | 配置编解码与独立设置控件 | 所有背景组合视觉验收 |
| 圆角裁剪、图片、边框共用几何；选中标记独立 | 宿主 Direct2D 裁剪和外框同圆角；独立选中描边和标点 | 透明背景、圆角边缘、边框和阴影实测 |
| 默认浮动标题不移动原图、不占固定行、不抢点击 | 独立绘制层，无命中对象；下方优先、上方回退、页面边界限制 | 长标题、屏幕边缘、键盘聚焦、透明区域点击 |
| 整个矩形可选择、打开、菜单、拖动 | 大图标命中采用外框，保留原点击路径 | 混合框选、单击/双击打开次数和透明角落 |
| 拖动、缩放、菜单期间隐藏标题并暂停悬停 | 输入条件和标题绘制前置判断 | 快速交错操作无残留状态 |

## C：创建、跨度与容器

| 要求 | 当前证据 | 尚需完成或取得的证据 |
| --- | --- | --- |
| 2×2 原位置尝试；空间不足占位预览；合法任意跨度；Esc 不修改 | 创建检查、手柄、键盘占格、确认/取消路径 | 满页、1×1 页、相邻组件、取消及保存失败 |
| 移动、排序、跨页、跨屏保留跨度，非法主动落点拒绝 | 大图标群组边界检测和按完整跨度放置；翻页预览不迁移源 | 混合群组、名称/类型排序、多显示器拖动和取消 |
| 期望跨度与临时适配分开，显示环境恢复后重新尝试 | `LargeIconConfig` 与布局 `gridSpan` 分开；恢复尝试和碰撞安置 | 屏幕缩小、断开、重新接入、重启及冲突组合 |
| 收纳容器成功接收才退化；保留桌面源则保留原实例 | 容器成员关系提交后清除配置；Dock `keepOnDesktop` 排除源迁移 | Dock/悬浮 Dock/集合，拒收、取消、部分成功、保留源和拖回 |
| 应用打开文件与组件处理引用不退化 | 文件提交与收纳成员关系采用不同路径 | 真实应用文件打开及 Lua 引用处理对照 |

## D：详细设置与轻量效果

| 要求 | 当前证据 | 尚需完成或取得的证据 |
| --- | --- | --- |
| 现有 WinUI 设置进程，当前项目定位，三组设置，实时预览 | 私有路由、IPC、原生设置页及全部配置控件 | 独立设置窗口运行验证、缩放和键盘操作 |
| 原图/静态图片/Steam 内容、适配、焦点、标题、全部边框背景参数 | 配置字段清单与设置控件对应 | 预览和宿主最终显示的一致性 |
| 恢复原图保留大图标；还原普通图标始终可用 | 两个独立动作，修改与还原走不同授权条件 | 锁定状态及素材加载中执行还原 |
| 连续调整预览，结束提交；Esc 取消，失效取消未提交状态 | 滑块预览、颜色取消、宿主预览对象 | 补齐并验证所有连续控件的统一取消行为 |
| 左移显标题保留原尺寸、两行省略、空间不足回退浮动 | 宿主计算规则与窄框测试 | 快速进出、长标题、封面和减少动画对照 |
| 悬停主位移互斥，可叠加一种外框效果；按下和启动单次反馈 | 控件联动与宿主效果绘制 | 精确时序、反向连续性、启动只触发一次 |
| 设置预览完整反映外观及动效 | 当前预览显示悬停/按下结束状态 | 阴影及完整过渡时序的预览仍待补齐 |
| 服从全局动画、节能、帧率；静止与不可见不持续刷新 | 使用宿主动画开关与调度，结束取消刷新 | 状态机自动化与静止/隐藏场景性能证据 |

## E：素材获取与资源边界

| 要求 | 当前证据 | 尚需完成或取得的证据 |
| --- | --- | --- |
| Steam URL 两种协议，拒绝非商店长 ID | AppID 解析及边界测试 | 真实快捷方式与商店不存在的合法 ID |
| 自有缓存、本地旧平铺/新目录/哈希目录优先 | WIC 素材测试含三类目录；新版 `library_capsule` 已补齐 | 更多实际 Steam 客户端缓存样本 |
| 元数据真实路径、横竖、高分辨率、语言回退 | 新测试完整走语言回退、元数据、哈希下载、WIC、缓存；公开 AppID 570 元数据与两图 HTTP 200 | 宿主内真实服务加载及其他语言/游戏 |
| 仅本地无新请求，允许已缓存图片；手选图优先 | 素材测试检查 HTTP 次数及缓存复用；宿主请求代次保护 | 编辑中切换仅本地和选择自定义图的并发验证 |
| 源文件 16 MiB、1600 万像素、格式白名单、复制后使用 | 实际 WIC 解码和非法/超限文件测试，源字节保真 | BMP、JPEG、ICO 多帧专门样例及损坏缓存恢复 |
| 两任务上限，相同 AppID/类型/语言请求合并 | 可控制网络边界的并发及去重测试；共享失败后按监听项目分别回退，旧封面与真实 Shell 原图隔离测试通过 | 宿主中的多实例并发显示验证 |
| 失败保留旧图，404 24 小时，短暂失败退避，刷新可重试 | 素材失败缓存与刷新测试；宿主保留当前位图 | 各类下载失败、取消和刷新交错回归 |
| 128 MiB 解码预算、引用保护、未引用缓存回收，后台不阻塞桌面 | 扫描和排序移出请求锁，删除前重新校验引用；小预算下的活跃图片保护、释放后恢复、自动磁盘回收测试通过 | 实际大容量与慢磁盘性能；进一步核对自动文件命名边界 |
| 原图与美化缓存隔离，按显示尺寸/DPI，资源变更重算取色 | 原图服务独立；新测试验证 Shell 索引及源时间变化失效 | 相同快捷方式同时位于大图标与 Dock 的视觉对照 |
| 迟到回调不覆盖手选、已还原/删除/迁出实例 | 取消和代次测试；宿主身份和容器检查；已入完成队列的缓存命中取消测试通过 | 与编辑会话切换的宿主运行验证 |

## F：交付门槛

- 每轮编译成功按双语 `try` 提交记录，再开始下一轮原生代码调整；不将自动化当作实机验证。
- 新增界面文案同步全部 10 种语言，运行本地化契约，并人工检查翻译。
- 最终运行 `scripts/build.bat`、`scripts/test.bat full`；针对新增集成场景使用 CTest `integration` 标签。
- 桌面视觉、悬停、框选、拖放、Dock 由用户实机验证或稳定的等价可观察证据证明，不能调用桌面自动化绕过仓库限制。
- 当前还有上述明确代码缺口和待验证项，不能标记“全部规划已完成”。

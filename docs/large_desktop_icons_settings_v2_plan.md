# 大图标设置重构规划（第二版，实施中）

依据 2026-09-08 用户确认的设置草稿及标题粗细、颜色补充要求实施。本文替代旧规划中背景、内容、标题和动效的设置组织；具体完成情况以代码与验证记录为准。高级解锁、跨度、容器退化、单格拖动虚影、中键拖动、缩放停绘及组件手柄行为继续沿用。

可交互的页面草稿见 [HTML 设置页草稿](prototypes/large-icon-settings.html)。草稿使用普通图标、Steam 和窄框示例展示条件显示、控件联动及二级导航；仅为层级确认，不执行宿主配置保存、图标识别、Steam 下载或桌面效果渲染。

## 1. 页面结构与默认行为

页面采用组件设置同类的二级页结构，归属于“图标设置”。内容只保留背景、图标、视觉效果三个设置分组；移除常驻预览及占格尺寸。

- 顶部使用设置窗口已有的返回按钮、二级页标题及项目名称。导航关系为“图标设置 → 大图标设置（当前项目）”，在同一设置窗口内切换，不再作为没有父级的独立设置入口。
- 占格尺寸不出现在设置页，也不在折叠项中保留入口；外框尺寸通过桌面上的组件式缩放手柄调整。图标分组的“图标大小”仍保留，只控制内部图片比例。
- 删除设置页常驻预览及其占位空间。位置与裁切直接通过左右、上下两行控制，不另设九宫格或图上拖动编辑器。
- 三组采用同一套设置行，标签在左，编辑控件和逐项恢复按钮在右；连续数值统一使用“滑杆＋数值输入框＋恢复按钮”控件组，窄窗口换行但保持动作靠右。
- 只显示当前模式需要的子项，按从属关系缩进；不将背景、图标、视觉效果再拆成三级导航页面，不增加重复选择器或常驻高级参数列表。
- 每项重置恢复该项默认值；切换模式保留其他模式的已保存配置，不重置图片、裁切或手动位置。
- 普通项目默认：选择“默认”背景、原始前景图标居中、短边 60%、无视觉效果。“图标背景色”沿用图标美化的底色填充方式；主题色仅用于独立、互斥的渐变模式。
- Steam 游戏默认：图标填充、读取到的 Steam 背景图、裁切填充、无视觉效果。
- 普通项目切换到“图标填充”时，默认将原始图标等比放大、居中裁切铺满外框；此模式只绘制填充图片，不绘制组件背景或独立前景图标层。
- 大图标外框之外的悬浮标题全部取消。默认悬停不显示名称；名称仅在启用动态标题后于框内出现。保留设置页名称和无障碍名称。

### 1.1 二级页导航与返回

- 从桌面右键直接打开大图标设置，也必须建立“图标设置 → 当前大图标”的父子关系；即使设置进程首次启动、没有历史记录，返回按钮也能到图标设置页。
- 从设置窗口内进入时沿用统一导航机制。返回图标设置页恢复父页原有滚动位置和焦点；重复打开同一实例不重复堆叠相同历史项。
- 左侧导航保留图标设置所属项的选中状态，不新增与其并列的“大图标设置”一级入口。返回按钮与已有键盘返回操作使用同一规则。
- 选择另一实例时更新当前二级页的项目名称和编辑会话，返回目的地仍为图标设置页，不让返回键依次遍历先前编辑过的图标。
- 离开前结束连续调整并提交合法修改；保存失败时明确显示错误和重试入口，不静默丢弃用户操作。已删除、还原或迁入容器的实例退出失效会话，返回图标设置页。
- 复用组件二级页的页面容器、标题、间距和返回行为；不以复制组件的实例业务逻辑代替导航复用。

更新后的顶层草稿如下；背景、图标、视觉效果内部继续使用已列出的条件显示和逐项重置规则。

| 缩进级别 | 标题 | 控件 | 显示条件 |
| --- | --- | --- | --- |
| 页面导航 | 返回图标设置 | 现有返回按钮 | 当前大图标二级页，包括桌面右键直达 |
| 页面标题 | 大图标设置／项目名称 | 现有二级页标题区域 | 当前大图标二级页 |
| 0 | 背景 | 分组标题 | 始终显示 |
| 1 | 背景样式 | 单层下拉框：默认、图标填充、跟随组件、深色、浅色、毛玻璃深色、毛玻璃浅色、亚克力深色、亚克力浅色、自定义 | 始终显示 |
| 1 | 圆角 | 滑杆＋百分比数值框＋恢复按钮 | 所有背景样式 |
| 0 | 图标 | 分组及从属设置 | 背景不是“图标填充” |
| 0 | 视觉效果 | 分组及效果选择器 | 始终显示 |

导航和页面标题属于页面框架，不作为表单中的额外设置行。占格尺寸和常驻预览从草稿中删除。

### 1.2 统一数值控件与位置设置

- 圆角、大小、不透明度、模糊及效果强度、边框和高光宽度、字号、渐变角度及位置等连续数值使用同一控件组。数值框可以直接输入，单位明确；滑杆和数值框双向同步，恢复按钮只恢复当前行。
- 拖动滑杆期间沿用即时展示、结束提交规则；数值输入确认后提交，非法输入不写入配置。不同显示条件不改变已保存值。
- 位置类设置统一拆为“左右位置”和“上下位置”。前景图标的位置与填充图片的裁切位置分别保存、互斥显示，不出现两套同时控制同一图片的入口。
- 图标位置按可移动范围归一化：0% 对应左端或顶端，100% 对应右端或底端，50% 居中；以图标实际尺寸和可用区域计算，不让 0% 或 100% 把图标移出外框。
- 裁切位置按对应方向的裁切余量归一化，默认 50% 居中。没有裁切余量的轴置灰，说明无需调整；完整显示时隐藏两行裁切设置。
- 渐变角度使用单条滑杆及角度数值框，不再使用方向盘；渐变范围分别使用起始位置和终止位置两行控件。
- 非连续选择仍保留合适控件：开关、模式下拉框、颜色选择器和文件选择按钮。动态标题的左移／上移属于效果类型选择，使用下拉框，不转换成任意位置偏移。

| 缩进级别 | 标题 | 控件 | 显示条件 |
| --- | --- | --- | --- |
| 1 | 图标大小 | 滑杆＋百分比数值框＋恢复按钮 | 图标分组显示时 |
| 1 | 左右位置 | 滑杆＋百分比数值框＋恢复按钮 | 图标分组显示时；动态标题接管位置时置灰 |
| 1 | 上下位置 | 滑杆＋百分比数值框＋恢复按钮 | 图标分组显示时；动态标题接管位置时置灰 |
| 3 | 左右位置 | 滑杆＋百分比数值框＋恢复按钮 | 图标填充采用裁切填充；没有横向裁切余量时置灰 |
| 3 | 上下位置 | 滑杆＋百分比数值框＋恢复按钮 | 图标填充采用裁切填充；没有纵向裁切余量时置灰 |
| 3 | 渐变角度 | 滑杆＋角度数值框＋恢复按钮 | 默认渐变或自定义渐变开启时；缺少主题色仍可手动编辑 |
| 4 | 起始位置 | 滑杆＋百分比数值框＋恢复按钮 | 展开手动渐变的更多设置 |
| 4 | 终止位置 | 滑杆＋百分比数值框＋恢复按钮 | 展开手动渐变的更多设置 |
| 2 | 效果强度 | 滑杆＋数值框＋恢复按钮 | 选择 3D 效果 |
| 2 | 标题字号 | 滑杆＋字号数值框＋恢复按钮 | 选择动态标题 |
| 2 | 标题粗细 | 滑杆＋数值框＋恢复按钮，100–900，步长 100 | 选择动态标题 |
| 2 | 自动标题颜色 | 开关＋恢复按钮 | 选择动态标题；组件背景系列说明默认跟随主题前景色 |
| 3 | 标题颜色 | 颜色选择器＋恢复按钮 | 选择动态标题，且关闭自动标题颜色 |

渐变色点的不透明度和位置同样使用该控件组；添加、删除色点保留按钮操作，色点列表按需展开。

## 2. 背景

统一使用一个“背景样式”单层选择器，按以下顺序直接平铺：默认、图标填充、跟随组件、深色、浅色、毛玻璃深色、毛玻璃浅色、亚克力深色、亚克力浅色、自定义。原“智能识别”选项改名为“默认”。

“现有组件预设”只是规划中的统称，不是界面里的选项或子菜单；具体预设直接从现有组件预设清单展开，自定义去重并放在最后。不得先选择“组件预设”再显示第二个预设选择器。手动渐变归入“自定义”下的“启用渐变”，不再单列“渐变色”背景样式。选择模式后的必要参数仍在当前页下方按需显示，不跳转额外页面。

平铺只约束“背景样式”选择器，不取消各模式自己的编辑能力。“默认”的图标背景色、识别，以及渐变和纯色的完整参数仍直接在默认模式下显示；“图标填充”的素材来源、Steam、本地选图、适配和裁切位置仍直接在填充模式下显示。操作这些设置无需先改为自定义，也不得因编辑子项自动切换背景样式。自定义仍提供完整组件背景编辑；默认渐变和纯色复用相同控件。

| 缩进级别 | 标题 | 控件 | 显示条件 |
| --- | --- | --- | --- |
| 3 | 智能识别填充图标背景色 | 开关及恢复按钮 | 默认模式开启图标背景色，且识别到可靠底色；关闭后仍显示 |
| 2 | 填充方式 | 三选一及恢复按钮 | 默认模式：图标背景色、渐变背景、纯色背景 |
| 3 | 背景不透明度 | 滑杆＋百分比数值框＋恢复按钮 | 图标背景色或纯色背景；可靠轮廓填充正在生效时置灰 |
| 3 | 渐变参数 | 完整组件渐变控件组 | 默认填充方式为渐变；支持全部颜色、透明度、范围和色点参数 |
| 3 | 背景颜色 | 颜色选择器＋恢复按钮 | 默认填充方式为纯色 |
| 2 | 填充图片来源 | 下拉框及恢复按钮 | 图标填充模式；仅 Steam 游戏提供 Steam 背景图选项 |
| 3 | 选择图片／更换图片 | 文件选择按钮 | 图标填充模式选择本地图片来源 |
| 3 | Steam 素材 | 素材缩略图选择器 | 图标填充模式选择 Steam 背景图来源 |
| 3 | 素材方向 | 下拉框及恢复按钮 | Steam 来源，且有对应素材选择 |
| 3 | 仅使用本地素材 | 开关及恢复按钮 | Steam 来源 |
| 3 | 刷新素材 | 按钮 | Steam 来源 |
| 3 | 素材状态 | 简短状态文字 | 正在获取、获取失败或正在使用回退图片 |
| 2 | 图片适配 | 下拉框及恢复按钮 | 图标填充模式 |
| 2 | 填充图片缩放 | 滑杆＋百分比数值框＋恢复按钮，25–300%，默认 100% | 图标填充模式；相对于适配结果缩放 |
| 3 | 左右位置 | 滑杆＋百分比数值框＋恢复按钮 | 图标填充采用裁切填充；没有横向裁切余量时置灰 |
| 3 | 上下位置 | 滑杆＋百分比数值框＋恢复按钮 | 图标填充采用裁切填充；没有纵向裁切余量时置灰 |

上述子项出现于同一个二级页内。圆角位于背景分组的独立行，默认、图标填充、跟随组件、各固定预设及自定义都可调整。

本轮扩展组件本身的背景设置及共享渐变引擎，大图标复用相同编辑器和参数解析。依赖图标素材的“默认”取色开关仍属于大图标设置；普通组件没有关联图标时不显示这些开关。组件全局外观和单实例自定义外观均应具备适用的渐变编辑能力，跟随全局的实例沿用现有继承关系。

| 样式 | 行为 | 展开的设置 |
| --- | --- | --- |
| 默认 | 普通项目默认。图标背景色、渐变背景、纯色背景互斥 | 图标背景色及识别；渐变和纯色提供完整参数，按模式披露 |
| 图标填充 | 纯图片展示，组件背景和独立前景图标层均不显示；普通项目默认放大裁切原图，Steam 默认使用读取到的背景图 | 图片来源、适配及裁切；按实际来源展开 Steam 或本地图片项 |
| 跟随组件 | 实时跟随当前全局组件背景方案，包括自定义方案的实际材质值 | 只显示当前方案名称及来源说明 |
| 各个固定组件预设（分别平铺） | 深色、浅色、毛玻璃深色、毛玻璃浅色、亚克力深色、亚克力浅色；选定后独立于全局切换 | 不展开内部参数或第二个预设选择器 |
| 自定义 | 复用组件材质编辑能力及扩展后的渐变设置 | 材质及必要参数；启用渐变后展开起始色、终止色、不透明度、角度及按需披露的范围、色点 |

### 2.1 接入组件背景引擎

- 组件已有 `DrawWidgetPanelBackground`，支持显式 `PersonalizationSettings`、独立圆角、材质、原生背景模糊、亚克力噪点与边缘高光。
- 使用组件预设、跟随组件及其他非图片背景时，大图标接入这条共享渲染链；将旧大图标自己的背景、描边、阴影配置入口归并到背景材质及视觉效果，避免同一外观有两套控制来源。
- 默认取色、可配置渐变和图片填充通过统一的背景内容解析选择绘制路径。图片填充分支仅复用外框、圆角裁剪及资源生命周期，不调用组件背景材质绘制或注册其原生模糊层，不附带组件底色、材质描边、噪点或高光。
- 进入图片填充时撤销该实例此前的组件背景合成请求，隐藏独立前景图标层；切回其他模式后恢复对应材质及前景配置。选择标记、焦点和命中区域仍由独立交互层负责。
- “跟随组件”是实时引用：全局组件方案更新后重新解析当前大图标背景。选择具体预设是独立选择，不随全局切换。
- 圆角始终由大图标实例持有。背景预设切换及全局背景更新不得覆盖圆角；不得将大图标设置写回全局组件设置。
- 组件与大图标使用相同的背景描述和预设解析。大图标设置页不再绘制常驻预览；毛玻璃、亚克力、圆角和高光通过桌面实际展示验收，不能用近似纯色代替实际材质。

### 2.2 “默认”背景与取色优先级

默认下提供互斥的三种填充方式：图标背景色、渐变背景、纯色背景。初始使用图标背景色及智能识别，回退不透明度 65%。

| 填充方式 | 可设置项 | 行为 |
| --- | --- | --- |
| 图标背景色（默认） | 检测成功后显示智能识别开关；回退底色不透明度 | 沿可见轮廓取色并不透明填充；失败或关闭识别时使用默认美化的 `#E8ECF4` 底色，初始 65% |
| 渐变背景 | 起止颜色及各自不透明度、方向、交换颜色、范围、最多五个色点；材质、模糊、边框、高光、前景色 | 初始由图标主题色生成；直接编辑后保留完整渐变，另可恢复图标主题色渐变 |
| 纯色背景 | 背景颜色、不透明度；材质、模糊、边框、高光、前景色 | 直接调整纯色填充，不被轮廓色覆盖 |

- 三种方式互斥，切换后保留各自参数；渐变与纯色的完整控件直接在“默认”下披露，无需切换到自定义。
- 大图标与图标美化共用 `icon_beautify::DetectPlateFill` 可见轮廓算法，不套用美化处理链。Shell 原图在编码前统一透明度格式；缓存键更新以重建旧的异常边缘资源。
- `defaultBackground` 保存三种方式；`defaultGradient` 保存手调渐变，`defaultSolidColor/defaultSolidOpacity` 保存纯色。旧 `themeColor/themeGradient` 配置迁移到对应方式；无新增组件 API 或 capability，旧宿主回退依赖升级前完整备份。

### 2.2.1 扩展组件渐变设置

现有组件页面提供“底部渐变”和 `gradientEndA` 终点不透明度。本轮规划扩展为共享的完整渐变描述，不将旧字段直接改名后赋予不兼容的新语义。

| 设置 | 规划 |
| --- | --- |
| 启用渐变 | 组件和大图标均在自定义背景中显示此开关，开启后才展开共享渐变编辑项 |
| 起始色、终止色 | 独立选择颜色，每个端点的不透明度单列滑杆＋数值框＋恢复按钮，不同时提供另一套重复透明度入口；支持交换起止颜色及其不透明度 |
| 方向 | 使用“渐变角度”滑杆＋数值框＋恢复按钮，单位为度；0° 向右，90° 向下；不再设置另一套方向选择器 |
| 交换起止颜色、更多渐变设置 | 均采用左标题、右控件的标准设置行；交换为按钮，更多设置为展开开关 |
| 渐变范围 | 高级项，起始位置、终止位置分别使用滑杆＋百分比数值框＋恢复按钮；默认 0–100% 覆盖全框，要求起始位置小于终止位置 |
| 中间色点 | 按需添加和删除；每个色点通过颜色选择器及不透明度、位置两行数值控件编辑；默认仅保留起止两点，建议最多 5 个色点 |
| 重置 | 每项恢复默认值，整条渐变可重置为两端、全范围及默认方向；重置不改变独立圆角 |

- 本版按静态线性渐变规划；常规界面只显示端点和方向，中间色点、范围按需展开，避免将全部参数常驻平铺。
- 手动渐变以色点不透明度为唯一透明度来源，不再叠加一套同义的“整体背景透明度”滑块。默认渐变以图标主题色生成初始值；编辑任意渐变参数后独立保存全部色点，不受后续取色覆盖。
- 渐变描述与组件材质分开：纯色材质直接使用渐变填充，毛玻璃或亚克力自定义材质将其作为相同模糊层上的着色；保留原材质的噪点、高光及圆角规则。
- 固定组件预设保持原外观；进入自定义后可编辑。跟随组件的大图标同步全局渐变，独立背景实例保持自己的值。
- 新设置只改自己对应的组件外观范围，不借助共享结构意外改动 Dock、任务栏等使用方；它们继续沿用已有数据及渲染语义。
- 渐变停止时不产生持续刷新；尺寸改变只重新计算渐变几何，颜色配置不被改写。

### 2.3 图标填充和 Steam

- 普通项目提供“原始图标 / 本地图片”来源，首次开启默认原始图标；Steam 游戏额外提供“Steam 背景图”，默认使用素材服务读取到的背景图片，不再叠加游戏原图标。
- “图标填充”是独立的纯图片模式；“Steam 背景图”是这个模式内的素材来源，两者不在页面其他位置重复出现。
- 默认等比放大并居中裁切，铺满整个外框。提供独立填充缩放 25–300%，默认 100%，相对于适配结果缩放；缩小后空余区域保持透明，放大后重新计算裁切余量。此处原图作为填充素材使用，不受前景图标 60% 比例或低分辨率图标不放大的规则限制。
- 本地图片：选择、更换、恢复默认来源；沿用静态格式 PNG、JPEG、BMP、ICO 和宿主管理的导入副本。
- 适配提供“裁切填充 / 完整显示”。默认裁切填充；只有实际存在可裁切方向时才启用相应焦点调整。图片自身透明区域及完整显示的留白保持透明，不补组件背景、智能底色或其他底板。
- 裁切采用“左右位置”和“上下位置”两行滑杆＋百分比数值框＋恢复按钮，分别控制横向及纵向裁切焦点；不再提供九宫格、图上拖动或另一套重复偏移入口。默认及恢复值均为 50% 居中。
- 只有来源为 Steam 背景图时才展开实际可用素材的方向、真实缩略图、来源状态、仅本地和主动刷新；选项以实际读取到的素材为准。
- 自动来源继续本地优先、联网补齐，沿用语言回退、失败缓存、限流和陈旧结果隔离。手选图片优先，迟到的 Steam 结果不得替换它。
- 素材失败保留最近可用图；Steam 背景从未成功取得时，暂用原始图标放大裁切作为同一图片层的兜底，背景到达后替换。原图也未就绪时保持图片区域透明，不能重新显示组件背景或独立前景图标层。
- 开启后隐藏组件材质相关设置和整个前景图标设置分组，仅保留填充来源、适配、裁切、独立圆角及适用的视觉效果；此前的材质和前景配置保留，退出后恢复。

### 2.4 独立圆角

- 圆角独立于背景模式，在背景分组固定位置显示，所有样式都可调整。
- 沿用 0–100% 表述，100% 对应短边的一半；新建及重置匹配当前组件圆角，之后独立保存。
- 背景填充、图片、描边和动效遮罩共用最终圆角几何。

## 3. 图标

本分组仅控制背景之上的前景图标，不承担背景图片设置。

| 项目 | 规划 |
| --- | --- |
| 图片替换 | 原始图标或本地静态图片；提供更换与恢复原始图标，打开目标和项目名称不变 |
| 大小 | 相对外框短边的比例，默认 60%；保持宽高比；原始低分辨率图标保留不强行放大的规则 |
| 左右位置 | 滑杆＋百分比数值框＋恢复按钮，0–100% 对应可用水平移动范围，默认 50% 居中 |
| 上下位置 | 滑杆＋百分比数值框＋恢复按钮，0–100% 对应可用垂直移动范围，默认 50% 居中 |
| 图片填充背景 | 隐藏整个图标设置分组并停止绘制前景图标层；前景图标及其配置保留，切回其他背景后恢复 |
| 动态标题效果 | 静止位置固定居中；位置控件禁用并说明由效果控制，退出动态标题后恢复已保存的手动位置 |

前景替换与背景填充必须保存为不同素材引用。更换封面不能覆盖前景替换图，切回“默认”也不能把整张封面误当作前景图标。

## 4. 视觉效果

统一为三选一：“无效果 / 3D 效果 / 动态标题”。不保留旧的悬停位移、外框增强、按下、启动、封面悬停等一长串组合开关。普通选择、焦点和点击语义继续保留。

### 4.1 无效果（默认）

维持当前背景和图标外观，不发生悬停位移、倾斜或标题出现。不展开任何效果参数。

### 4.2 3D 效果

采用草稿中的“随鼠标轻微倾斜的卡片，带光影变化”。该方案随“其他没问题了，开始实施”一并进入实施范围。

- 鼠标进入后根据相对位置产生轻微透视倾斜，移出平滑复位；默认无名称显示。
- 页面只展开“效果强度”，使用滑杆＋数值框＋恢复按钮，不再提供轻微、标准、明显的分段选择；具体强度范围随 3D 效果定义确定。
- 物理占格、鼠标命中框和碰撞规则固定；背景、原生模糊、内容、圆角及边框要在同一几何下呈现。
- 原生毛玻璃/亚克力背景涉及独立合成层，3D 与其几何一致性必须先做技术验证，再确定具体绘制路径。不能只倾斜图标却宣称完成整卡片效果，也不能悄悄把材质改成纯色。
- 3D 内容已接入 Direct2D 整框投影；原生模糊使用相同矩阵与投影区域，实际桌面对齐仍待实机验证。

### 4.3 动态标题

- 方向默认“自动”，同时支持指定“向左移动，右侧显示标题”或“向上移动，下方显示标题”。自动模式优先横框左移、竖框和方框上移，当前方向放不下实际图标和标题时尝试另一方向。
- 静止状态图片居中，无常驻标题；悬停后图片移入左侧/上侧区域，文字在腾出的右侧/下方居中出现。
- 前景图标模式：背景保持不动，前景保持原尺寸移动；空间不足时禁用对应方向，不为塞标题强行缩小原图。
- 图片填充模式不支持动态标题：选项置灰、参数隐藏、停止相关动画。原有动态标题参数保留供切回图标层使用。
- 原始图的完整展示规则与图片填充的裁切规则分开判断可用性。横向和纵向分别计算，不能因为“向左”不适用就禁用“向上”。
- 标题为大字，默认 24 CU；独立字号和粗细使用滑杆＋数值框＋恢复按钮。粗细为 100–900，步长 100，默认 600。最多两行，超长省略，无独立背景胶囊。
- 标题颜色默认自动：跟随组件、固定组件预设和自定义组件材质使用实际解析后的主题前景色；默认背景及图片填充按承载区域选择可读颜色。关闭自动后显示手动颜色选择器，切换自动/手动保留手动颜色。各项可独立重置。
- 超长名称不再弹出完整悬浮提示。需要完整名称时使用设置顶部名称或原有项目属性；保留无障碍名称。
- 进入延迟、进入/退出时长先采用一致默认值，收进内部默认配置；首版页面不单独平铺毫秒参数。
- 快速反向悬停从当前姿态继续，不排队。键盘聚焦可以呈现相同框内标题；减少动画时直接切换静止/展开状态，不出现外部浮动标题。
- 用 DirectWrite 按实际名称、字号、字重测量一至两行标题。图标与标题优先按 12 CU 间距组成居中的紧凑组合；空间紧张时缩短边距与间距。位移只为实际文字腾出空间，图标尺寸保持不变。垂直于移动方向的图标边缘无需额外预留文字边距。
- 动态标题使用原动效时长的 60%：默认延迟 72 ms、进入 120 ms、退出 96 ms；叠加宿主动画速度设置，快速反向从当前进度继续。
- 缩小外框后，手动方向不可用则暂停；自动模式尝试可用方向，两者都不可用时暂停并说明原因；恢复足够尺寸后恢复。静默回退到已删除的悬浮标题不属于本方案。

## 5. 模式联动和取消规则

| 当前状态 | 界面及渲染规则 |
| --- | --- |
| 默认：图标背景色 | 可靠底色不透明填充，无可靠底色时采用默认美化底色与独立不透明度；与主题色渐变互斥 |
| 默认：渐变背景 | 完整渐变、材质、边框、高光和前景色设置，不叠加轮廓底色 |
| 默认：纯色背景 | 颜色、不透明度、材质、边框、高光和前景色设置 |
| 默认：取色关闭或不可用 | 使用默认底色，保留开关值；换图或资源就绪后重新解析 |
| 跟随组件 | 不展开本地材质参数；圆角和前景图独立 |
| 固定组件预设 | 不展开不生效的自定义参数；要编辑材质则进入组件已有自定义方式 |
| 自定义启用渐变 | 展开共享渐变编辑项，隐藏重复的纯色背景颜色和不透明度入口；背景样式仍为“自定义” |
| 图标填充 | 只绘制填充图片，隐藏组件材质设置及前景图标分组；图片和裁切入口只出现在背景分组，独立圆角继续可用 |
| Steam 自动来源 | 展开 Steam 项；选本地来源后收起 Steam 项，并阻止异步覆盖手选图 |
| 无效果 | 不展示效果子项或标题配置 |
| 3D 效果 | 仅展示 3D 强度；不展示动态标题项 |
| 动态标题 | 展示方向、字号、粗细及颜色；手动颜色按需展开，前景手动位置由效果暂时接管 |
| 空间不够 | 具体方向置灰并说明原因；恢复可用尺寸后解除 |
| 拖动、尺寸调整、菜单打开 | 暂停视觉效果，恢复静止姿态；缩放只画占位框，结束恢复 |
| 减少动画 | 3D 关闭；动态标题无过渡地展示框内终态；普通悬浮标题始终不存在 |

设置页标题、错误提示及重置按钮的控件说明不属于桌面悬浮标题，不随此次移除而删除。

## 6. 配置与兼容边界

将实例数据按职责归为 `frame`、`background`、`foreground`、`effect`，素材服务仍负责引用、下载、解码及缓存；实际字段名在实现前确定。

- `frame`：期望跨度、独立圆角。
- `background`：背景样式、默认模式取色开关及主题色参数、组件预设/自定义材质、共享渐变描述、填充来源及裁切、Steam 策略；识别结果是素材派生状态，不替代用户设置值。
- `foreground`：原始或替换图片、比例、手动位置。
- `effect`：无效果/3D/动态标题及选中效果的参数。
- 当前活动模式与各模式已保存的值分别保存，切换回来可恢复先前设置。
- 这不是仅调整页面：涉及组件背景接入、前景与背景素材拆分、标题绘制层删除和新增效果状态，需要显式迁移旧 `LargeIconConfig`。
- 保留期望跨度、独立圆角、原项目身份与素材；旧封面/自选填充图迁入背景，旧左移标题映射为动态标题向左，旧浮动标题关闭。
- 对旧手动背景及不能一一对应的效果先列迁移表，明确保留值和按本轮要求淘汰的行为，再编写迁移代码；不能简单套新建默认值覆盖全部实例。
- 背景结构发生实质变化，应提高大图标配置自身版本并保留旧版本读取/迁移；布局根版本保持。旧“智能识别”迁移到“默认”时保留原外观或明确记录采用新回退规则的变化，不能声称只是重命名。
- 组件渐变涉及全局外观、单实例自定义值、现有组件预览、宿主渲染、备份及已有主题读取方。实现前需要盘点现有 `gradientEndA` 在主题读取和组件设置中的使用，保留旧字段语义及缺少新字段时的旧外观；不能直接用全框双色渐变重解释旧“底部渐变”。大图标不因此恢复已删除的常驻预览。
- 本版不预先承诺新增 Lua API。若共享主题返回值、组件清单可写项或其行为需要扩展，须在实际编辑前明确列出公共 API 变化、受影响组件、feature/capability 或版本要求及旧宿主降级路径；仅宿主内部和设置私有数据扩展则保持现有组件 API。
- 修改存储格式前按仓库规则再次说明读取方、降级限制和完整备份方式。
- 解锁判断仍由宿主统一处理。失效后现有实例正常使用、不能改专属设置，仍可还原普通图标；打开目标、容器退化及备份资源规则沿用原规划。

### 6.1 v1 到 v2 的迁移表

| v1 状态 | v2 行为与保留值 |
| --- | --- |
| 原图＋自动背景 | 进入“默认”，采用可靠轮廓底色→默认美化底色的新规则；这会改变旧版混色外观，不能视为仅改名。旧不透明度迁为默认底色不透明度 |
| 原图＋手动背景 | 进入“自定义”普通材质，保留颜色、不透明度、边框及圆角 |
| 自选图片或 Steam | 进入“图标填充”，保留填充来源、图片引用、适配、裁切、Steam 偏好及缓存；不迁成前景替换图 |
| 左移标题 | 进入“动态标题／左移”，保留框内字号及标题颜色；新增字重默认 600 |
| 默认浮动标题 | 进入“无效果”，不再绘制框外标题 |
| 旧悬停、按下、启动和封面效果 | 旧参数保留在兼容字段中，新界面与渲染不再启用这些组合行为 |
| 尺寸、圆角及项目身份 | 保留期望跨度和当前适配跨度的区别；缺少相对圆角字段的旧实例继续按独立 CU 半径读取 |
| 组件底栏渐变 | 原 `gradientEndA` 继续只控制底栏；缺少新的 `panelGradient` 时整框渐变关闭 |

布局根版本不变，大图标对象版本为 2。新增可选 `fillScale` 和 `autoTitleDirection`；缺少时分别采用 100% 和旧配置的手动方向，新增实例默认自动方向。旧宿主会忽略新增字段。组件全局外观增加可选 `panelGradient` 对象，单实例外观将同一描述保存到宿主私有 `__panelGradient` 键，设置私有 IPC 版本升到 5；Lua 接口及组件 `apiVersion` 保持不变。缺少或损坏的实例渐变回退为关闭，不改变旧底栏渐变；现有组件预设关闭整框渐变并保留其配色供后续自定义。首次写入 v2 大图标布局前，创建独立的 `LargeIconV2UpgradeBackup` 完整备份；已有 `LargeIconUpgradeBackup` 不能代替它。两个备份不被后续普通保存覆盖。

回退旧宿主使用相应升级前完整备份；重新升级使用新版完整备份恢复。旧宿主不能保证保留新增字段，不承诺旧版编辑后的布局仍能无损恢复本版外观。

3D 实现依据 [Direct2D 3D transform](https://learn.microsoft.com/en-us/windows/win32/direct2d/3d-transform) 和 [Composition Visual](https://learn.microsoft.com/en-us/uwp/api/windows.ui.composition.visual)：内容投影与原生模糊面板需使用等价的矩阵和几何，实际对齐仍需运行时证据。

## 7. 实施顺序与验收

1. 固定上述页面职责、3D 定义及迁移表；接入以图标设置为父页的二级导航，移除占格尺寸和常驻预览，统一设置数据与适用性规则。
2. 扩展组件背景渐变描述与编辑器，保留旧渐变兼容；接入大图标跟随/固定预设和独立圆角，完成“默认”的取色回退链及图片填充。
3. 拆分背景与前景素材，重建图标设置，保证模式切换可恢复且不串图。
4. 删除全部桌面大图标悬浮标题路径，完成动态标题两个方向及可用性判断。
5. 验证并实现选定的 3D 方案，统一动效、缩放停绘和拖动静止姿态。
6. 完成全语言文案、标准构建和与边界相匹配的测试，按仓库规则保存待实机验证的双语 try 提交。

验收重点：同预设与组件材质一致；切换全局背景时只有跟随实例变化；圆角不被预设覆盖；轮廓识别项条件显示且关闭后可以再开启，轮廓填充始终不透明；图标背景色与主题色渐变互斥并使用各自的取色规则及设定的不透明度，失败底色一致；手动渐变的颜色、方向、范围及色点在预览、组件、大图标和重启后相同，旧底部渐变保留原语义；图片填充时组件底色、模糊层和前景图标均消失，透明源图没有残留底板，填充模式禁用动态标题；普通图默认放大裁切且 Steam 使用读取到的背景；Steam 与本地图片入口正确联动；背景和前景图片独立；左/上标题各自置灰、居中、无衬底；任何大图标模式均无外部悬浮标题；3D 不产生材质或命中错位；锁定、重启、备份恢复与旧配置迁移保持合法状态。

导航与控件验收补充：设置进程首次启动后从桌面右键直达、已有设置窗口中直达、设置页内进入均可返回图标设置；重复打开同一实例或切换实例不产生重复历史；返回后恢复父页位置；页面没有占格尺寸和常驻预览；连续数值滑杆、数值输入和逐项恢复保持同步；左右/上下位置独立且默认居中，图标不越出外框；裁切轴无余量时不可调，不显示九宫格或另一个裁切编辑入口。

本轮只检查现有代码与形成规划，未构建或运行测试。规划中建议、暂定定义和待技术验证事项均不代表当前功能已经实现。

### 本轮交互校正

- 动态标题仅支持非图标填充的独立图标层。图标填充下选项置灰、参数隐藏、已有标题动画停止；保留参数供切回图标层后使用。
- 图标增大时先缩短空隙与边距；图标在垂直于移动方向的一边可贴近外框，不因预留文字边距而误禁用。仍保持图标尺寸，不强行缩小。
- 3D 鼠标跟随使用最长 65 ms 的缓出，退出最长 100 ms，不叠加悬停延迟，仍服从全局动画速度与减少动画设置。
- 大图标拖到无效落点时按原跨度显示与组件相同的红色提示；混合选择保留相对偏移，拒绝落点后状态不变。
- 文件选择器由设置进程以设置窗口为所有者打开；宿主只接收路径并执行授权验证和异步导入。首次选图等待期间重绘不得替换导入请求。
- Steam `.url` 非填充模式优先读取 `IconFile` 与 `IconIndex`，支持相对路径、环境变量与 ICO 最大有效帧；不使用封面替代原图。

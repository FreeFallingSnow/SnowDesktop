# SnowDesktop Lua 组件 API v2 测试验收表

本文档用于逐项验证 SnowDesktop Lua 组件宿主 API。清单以
[`src/widget_public_api.inc`](../src/widget_public_api.inc) 为唯一函数目录基准，覆盖当前公开的
**20 个宿主库、168 个函数 API**。接口参数、返回值和数据结构以
[`widgets/snowdesktop-lua-widget/library/snowdesktop-v2.lua`](../widgets/snowdesktop-lua-widget/library/snowdesktop-v2.lua)
为准，详细行为说明参见
[`widgets/snowdesktop-lua-widget/references/api-v2.md`](../widgets/snowdesktop-lua-widget/references/api-v2.md)。

> 范围说明：本表验收宿主公开的 Lua 函数，不把 Lua 自带的 `string`、`table`、`math`、`utf8`
> 函数逐项计入。`data.subscribe` 的 topic、`task.start` 的任务 ID、声明式节点属性和事件载荷属于
> 数据/视图契约，应分别结合 `snowwidget system-contract` 和 `snowwidget view-contract` 的导出继续
> 做参数化验收。

## 验收记录

| 项目 | 填写内容 |
|---|---|
| SnowDesktop 版本 | 1.0.4.0 |
| Commit / 构建号 | `release/v1.0.4.0`；系统状态 `647428d`；原生滚动 `c3006e0`；性能测量 `dc3b9b9`；lint 修复 `9ea17a5` |
| 操作系统与版本 | Windows 11 家庭中文版 10.0.26200（build 26200） |
| DPI / 显示缩放 | 96 / 120 / 144 DPI |
| 主题 / 高对比度 | 深色、浅色；高对比度待后续批次 |
| 测试组件与版本 | 内置 `system-monitor` 2.3.0；非内置 `performance-history` 1.0.0 |
| 测试人员 | Codex 自动化、性能采样与用户实机验收 |
| 测试日期 | 2026-08-20 |

结果栏填写：`P`（通过）、`F`（失败）、`B`（阻塞）或 `N/A`（本轮不适用）。每个 `F` 或 `B`
必须在证据/备注栏填写日志、截图、缺陷号或复现步骤。除纯查询接口外，每项至少覆盖一次有效输入和
一次无效/边界输入；涉及宿主状态、可信手势、权限或资源生命周期的接口，还应验证拒绝路径和资源回收。

## 总体验收门槛

- [x] `snowwidget api-contract` 返回 `ok:true`、`apiVersion:2`，函数总数为 168，且与本表名称逐项一致。
- [x] `snowwidget lint <组件目录>` 能识别不存在、版本不匹配或缺少权限声明的调用。
- [x] `snowwidget preview <组件目录> <输出.png>` 能覆盖预览适用的绘制、视图、资源和状态接口。
- [ ] 在真实桌面宿主中完成可信手势、辅助 surface、权限、系统数据、异步任务和生命周期测试。
- [ ] 错误输入返回稳定错误或拒绝结果，宿主不崩溃、不泄漏资源，上一棵有效视图不被无效视图替换。
- [x] `scripts/test.bat` 完整测试通过；若未运行，必须在总结中注明原因。

## 分批执行记录

全表结果栏只有在满足对应条目的有效输入、边界/拒绝路径和生命周期要求后才填写 `P`。
下表用于记录尚未达到整项最终验收门槛、但已经取得的阶段性证据，避免把“调用成功”误写成
“接口全部通过”。

| 批次 | 测试对象 | 本批实际覆盖 | 阶段结果 | 证据 | 待补验收 |
|---|---|---|---|---|---|
| 01 | 内置 `system-monitor` 2.1.0 | CPU、内存、GPU、电源、网络状态/流量、存储卷/I/O 数据订阅；`system.uptime`；本地化数字/容量/时长/列表；即时绘制、滚动和数据状态包络 | 自动化与预览通过 | `snowwidget lint/validate/preview`；ready/loading/stale/permission-denied；2×1/3×2、96/144 DPI、深浅主题；`scripts/test.bat` 76/76；`scripts/build.bat --reload-shell` | 真实机器数据、现场滚动/菜单和长时间运行 |
| 01 | 非内置 `performance-history` 1.0.0 | `module.require`；六个 `state` API；`schedule.every`；CPU/内存/GPU/磁盘/下载订阅；`view.box/row/column/scroll/text/badge/sparkline/lineChart`；range/multiSelect 设置 | 自动化与预览通过 | 4/4 包内 Lua 测试；ready/stale/permission-denied/empty；2×2/3×2、96/120/144 DPI、3 个 locale、深浅主题；validate/pack；开发候选同步到 `data/widgets/dev` | 真实桌面实例的采样累积、右键重置、设置变更和权限授权流程 |
| 01 | `snowwidget lint` | 合法 `module.require()` 与禁用全局 `require()` 的区分 | 缺陷已修复并回归通过 | `SnowDesktopWidgetAuthorLintTests.exe`；实际组件 lint 0 错误/0 警告；提交 `9ea17a5` | 无 |
| 02 | 内置 `system-monitor` 2.3.0；`draw.marqueeText` | 宿主原生连续滚动、裁剪、位置、相位续接，以及不再以 33 ms 定时器逐帧重绘桌面；实机 CPU/GPU 稳态采样 | 实机性能与视觉通过 | `c3006e0`；`scripts/build.bat --reload-shell`；`scripts/test.bat` 76/76；稳定等待 8 秒后采样 46.79 秒：整机 CPU 2.542%→0.188%，单核等效 63.3%→4.51%，GPU 峰值约 2%→0.25%；用户确认滚动平滑、位置与裁剪无问题 | `draw.marqueeText` 的隐藏暂停、reduced-motion 和合成失败回退仍随 D-19 完整矩阵补验 |

> 历史组件边界：批次 01 执行时，`performance-history` 的可追踪源码位于
> `developer_assets/workshop_widgets/performance-history`，运行时仅同步到 `data/widgets/dev`。
> 该开发组件现已从仓库移除；上表仅保留当时的验收证据，它从未属于 `widgets/` 中的内置组件。

## 1. `draw` 即时绘制 API（19 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| D-01 | `draw.text` | 在即时绘制画布中绘制文本；核对位置、字号、颜色、裁剪及空文本/长文本行为。 | v1 | — |  |  |
| D-02 | `draw.measureText` | 测量文本占用尺寸；核对结果与实际绘制、字体和字号一致。 | v1 | — |  |  |
| D-03 | `draw.rect` | 绘制实心矩形；核对坐标、尺寸、颜色、透明度和圆角边界。 | v1 | — |  |  |
| D-04 | `draw.arc` | 绘制圆弧；核对起止角、线宽、颜色及退化半径处理。 | v2 | — |  |  |
| D-05 | `draw.path` | 绘制或填充路径；核对路径命令、闭合、填充规则及非法数据拒绝。 | v2 | — |  |  |
| D-06 | `draw.gradientRect` | 绘制渐变矩形；核对方向、端点颜色、透明度和裁剪。 | v2 | — |  |  |
| D-07 | `draw.shadow` | 绘制有界阴影；核对模糊、偏移、透明度及边界参数。 | v2 | — |  |  |
| D-08 | `draw.sparkline` | 绘制迷你趋势线；核对采样值、范围、线宽以及空/单点数据。 | v2 | — |  |  |
| D-09 | `draw.pushClip` | 压入绘制裁剪区；核对嵌套裁剪和越界内容不可见。 | v1 | — |  |  |
| D-10 | `draw.popClip` | 弹出最近裁剪区；核对裁剪恢复及栈不平衡时的安全处理。 | v1 | — |  |  |
| D-11 | `draw.strokeRect` | 绘制矩形边框；核对线宽、颜色、圆角和像素对齐。 | v1 | — |  |  |
| D-12 | `draw.line` | 绘制线段；核对端点、线宽、颜色及零长度线段。 | v1 | — |  |  |
| D-13 | `draw.circle` | 绘制圆形；核对圆心、半径、填充/边缘和非法半径。 | v1 | — |  |  |
| D-14 | `draw.fa` | 使用宿主 Font Awesome 字体绘制图标字符；核对字形、尺寸与颜色。 | v1 | — |  |  |
| D-15 | `draw.fluent` | 使用宿主 Fluent 图标字体绘制字形；核对字体变体、尺寸与颜色。 | v1 | — |  |  |
| D-16 | `draw.image` | 绘制已声明的包内图片资源；核对加载成功、透明通道和无效句柄。 | v1 | — |  |  |
| D-17 | `draw.imageFit` | 按填充、包含、覆盖或原尺寸策略绘图；核对对齐与插值模式。 | v2 | — |  |  |
| D-18 | `draw.icon` | 绘制宿主管理的桌面对象引用图标；核对授权、失效引用和拒权路径。 | v1 | `desktop.read` |  |  |
| D-19 | `draw.marqueeText` | 提交宿主管理的单行溢出滚动文字；核对不溢出时静态绘制、连续循环、稳定 key 相位、局部重绘、隐藏暂停、预览/reduced-motion 静态降级、数量与参数边界，以及滚动帧不重新进入 Lua。 | v2 | — |  | `system-monitor` 2.3.0 的实机连续滚动、裁剪、位置和降载已通过；完整边界与生命周期矩阵待补。 |

## 2. `interaction` 即时交互 API（6 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| I-01 | `interaction.region` | 注册即时绘制命中区域及动作、菜单、光标和无障碍语义；核对事件目标和重叠命中。 | v2 | — |  |  |
| I-02 | `interaction.isHovered` | 查询指定区域当前是否悬停；核对进入、离开和区域消失后的状态。 | v2 | — |  |  |
| I-03 | `interaction.isPressed` | 查询指定区域当前是否按下；核对按下、释放、取消和移出区域。 | v2 | — |  |  |
| I-04 | `interaction.isFocused` | 查询指定区域是否获得宿主焦点；核对键盘导航和焦点转移。 | v2 | — |  |  |
| I-05 | `interaction.scroll` | 声明并读取宿主管理的滚动区域；核对滚轮、偏移钳制和内容范围变化。 | v2 | — |  |  |
| I-06 | `interaction.setScrollOffset` | 设置指定滚动区域偏移；核对成功值、上下界钳制和未知区域。 | v2 | — |  |  |

## 3. `view` 声明式视图 API（48 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| V-01 | `view.box` | 创建通用单容器节点；核对尺寸、内外边距、样式和子节点约束。 | v2 | — |  |  |
| V-02 | `view.row` | 创建横向线性布局；核对间距、对齐、伸缩、换行和 RTL 行为。 | v2 | — |  |  |
| V-03 | `view.column` | 创建纵向线性布局；核对间距、对齐、伸缩、换行和溢出。 | v2 | — |  |  |
| V-04 | `view.grid` | 创建网格布局；核对固定/自动/比例轨道、格位、跨度和隐式行。 | v2 | — |  |  |
| V-05 | `view.flow` | 创建横向流式换行布局；核对换行点、行列间距和对齐。 | v2 | — |  |  |
| V-06 | `view.stack` | 创建层叠布局；核对 `offset`、`zIndex`、绘制顺序和重叠命中。 | v2 | — |  |  |
| V-07 | `view.scroll` | 创建宿主管理的滚动视口；核对方向、初始目标、滚动范围和事件。 | v2 | — |  |  |
| V-08 | `view.list` | 创建普通列表；核对方向、选择、空态/加载态和滚动组合。 | v2 | — |  |  |
| V-09 | `view.gridList` | 创建网格列表；核对网格排布、列表项语义、选择和内容状态。 | v2 | — |  |  |
| V-10 | `view.virtualList` | 创建只物化可见项的虚拟列表；核对大数据量、索引、滚动和可变尺寸。 | v2 | — |  |  |
| V-11 | `view.virtualGrid` | 创建虚拟网格；核对列数、固定项高、物化范围和大数据量滚动。 | v2 | — |  |  |
| V-12 | `view.virtualRange` | 计算宿主虚拟集合当前应物化的索引范围；核对空集、越界和 overscan。 | v2 | — |  |  |
| V-13 | `view.scrollTo` | 将滚动视口定位到指定后代 key；核对各对齐策略和未知 key。 | v2 | — |  |  |
| V-14 | `view.scrollBy` | 按增量滚动指定视口；核对正负增量、边界钳制和返回状态。 | v2 | — |  |  |
| V-15 | `view.scrollToIndex` | 将虚拟集合定位到指定索引；核对首尾项、对齐及越界拒绝。 | v2 | — |  |  |
| V-16 | `view.listItem` | 创建具有列表语义的集合项；核对稳定 key、选择、交互和无障碍位置。 | v2 | — |  |  |
| V-17 | `view.text` | 创建文本节点；核对换行、省略、排版、locale、方向和无障碍文本。 | v2 | — |  |  |
| V-18 | `view.styledText` | 创建由多段样式文本/图标组成的节点；核对 span 样式、命中和额度。 | v2 | — |  |  |
| V-19 | `view.textInput` | 创建受控单行文本框；核对输入、选区、提交、只读、必填和验证状态。 | v2 | — |  |  |
| V-20 | `view.textArea` | 创建受控多行文本框；核对换行、滚动、Ctrl+Enter 提交和字节限制。 | v2 | — |  |  |
| V-21 | `view.searchBox` | 创建受控搜索输入框；核对输入、提交、取消、占位文本和清除行为。 | v2 | — |  |  |
| V-22 | `view.numberInput` | 创建受控数字输入框；核对最小/最大/步长、非法文本和数值事件。 | v2 | — |  |  |
| V-23 | `view.select` | 创建受控下拉选择器；核对展开、选项选择、占位、禁用项和焦点。 | v2 | — |  |  |
| V-24 | `view.image` | 创建包内图片节点；核对资源句柄、缩放、对齐、插值、着色和替代文本。 | v2 | — |  |  |
| V-25 | `view.referenceIcon` | 创建宿主管理引用的图标节点；核对实例自有引用、失效状态和替代文本。 | v2 | — |  |  |
| V-26 | `view.button` | 创建按钮；核对可信点击、键盘激活、禁用态、焦点态和动作载荷。 | v2 | — |  |  |
| V-27 | `view.link` | 创建链接样式动作控件；核对点击/键盘动作、字体和无障碍角色。 | v2 | — |  |  |
| V-28 | `view.toggle` | 创建受控开关；核对开关状态提议、禁用态、键盘和无障碍状态。 | v2 | — |  |  |
| V-29 | `view.checkbox` | 创建受控复选框；核对选中、未选、混合态以及状态变更事件。 | v2 | — |  |  |
| V-30 | `view.radioGroup` | 创建受控单选组；核对选项唯一性、键盘移动、禁用项和选择事件。 | v2 | — |  |  |
| V-31 | `view.slider` | 创建受控滑块；核对范围、步长、方向、键盘/UIA 操作和变更事件。 | v2 | — |  |  |
| V-32 | `view.icon` | 创建宿主图标字体节点；核对字形、字体族、尺寸、颜色和语义。 | v2 | — |  |  |
| V-33 | `view.iconButton` | 创建仅图标按钮；核对可访问名称、点击、键盘、状态样式和禁用态。 | v2 | — |  |  |
| V-34 | `view.shape` | 创建矩形、圆角矩形、圆或椭圆图形；核对尺寸、边框和填充。 | v2 | — |  |  |
| V-35 | `view.badge` | 创建徽标状态节点；核对文本、尺寸、主题颜色和无障碍语义。 | v2 | — |  |  |
| V-36 | `view.divider` | 创建水平或垂直分隔线；核对方向、厚度、尺寸和颜色。 | v2 | — |  |  |
| V-37 | `view.progressBar` | 创建进度条；核对确定/不确定状态、范围、方向和主题表现。 | v2 | — |  |  |
| V-38 | `view.progressRing` | 创建环形进度指示器；核对确定/不确定状态、范围、厚度和动画降级。 | v2 | — |  |  |
| V-39 | `view.meter` | 创建数值仪表；核对范围钳制、轨道、填充和语义值。 | v2 | — |  |  |
| V-40 | `view.sparkline` | 创建声明式迷你趋势线；核对样本范围、空/单点数据和额度。 | v2 | — |  |  |
| V-41 | `view.lineChart` | 创建折线图；核对采样点、显式范围、线宽、填充和裁剪。 | v2 | — |  |  |
| V-42 | `view.barChart` | 创建柱状图；核对正负值、范围、柱宽、间距和零基线。 | v2 | — |  |  |
| V-43 | `view.waveform` | 创建波形图；核对音频样本、范围、线条/填充和数据额度。 | v2 | — |  |  |
| V-44 | `view.spectrum` | 创建频谱图；核对频段值、范围、柱形表现和空数据。 | v2 | — |  |  |
| V-45 | `view.monthCalendar` | 创建受控月历；核对年月、周起始日、相邻日期、事件标记和选择。 | v2 | — |  |  |
| V-46 | `view.slotSurface` | 创建与 manifest 逻辑槽位绑定的宿主表面；核对槽位种类、空态和拖放。 | v2 | — |  |  |
| V-47 | `view.slotItem` | 创建逻辑槽位中的宿主对象项；核对 opaque 引用、选择、图标和拖放样式。 | v2 | — |  |  |
| V-48 | `view.spacer` | 创建只占布局空间的空白节点；核对固定、填充和 flex 尺寸。 | v2 | — |  |  |

## 4. `widget` 组件生命周期与宿主 surface API（17 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| W-01 | `widget.info` | 获取兼容版实例摘要；核对组件 ID、尺寸、选择状态和当前包信息。 | v1 | — |  |  |
| W-02 | `widget.context` | 获取当前 surface 的尺寸、DPI、主题、区域、时区、可见性和辅助功能上下文。 | v2 | — |  |  |
| W-03 | `widget.hasPermission` | 查询当前实例是否已获某项权限；核对已授权、拒绝、未声明和未知权限。 | v1 | — |  |  |
| W-04 | `widget.setTitle` | 更新宿主显示的组件标题；核对正常文本、空值、长度限制和刷新。 | v1 | — |  |  |
| W-05 | `widget.openSettings` | 打开当前组件设置页；核对可信操作、重复调用和无设置项场景。 | v1 | — |  |  |
| W-06 | `widget.openPanel` | 打开辅助面板 surface；核对尺寸选项、重复打开、渲染与输入。 | v1 | — |  |  |
| W-07 | `widget.openDialog` | 打开居中对话框 surface；核对遮罩、Escape/外部关闭策略和输入隔离。 | v2 | — |  |  |
| W-08 | `widget.openPopover` | 从可信桌面手势打开锚定浮层；核对 anchorKey、方向选择、钳制和拒绝路径。 | v2 | — |  |  |
| W-09 | `widget.closePanel` | 关闭当前辅助面板；核对关闭事件、资源回收和无面板时调用。 | v1 | — |  |  |
| W-10 | `widget.closeDialog` | 关闭当前对话框；核对关闭事件、焦点恢复和无对话框时调用。 | v2 | — |  |  |
| W-11 | `widget.closePopover` | 关闭当前浮层；核对关闭事件、焦点恢复和无浮层时调用。 | v2 | — |  |  |
| W-12 | `widget.invalidate` | 请求宿主重新执行绘制/视图回调；核对合并调度和卸载后安全性。 | v1 | — |  |  |
| W-13 | `widget.log` | 写入组件分级日志；核对级别、文本上限、诊断归属和敏感信息处理。 | v1 | — |  |  |
| W-14 | `widget.theme` | 获取兼容版主题快照；核对深浅色、透明度、边框和内容主题。 | v1 | — |  |  |
| W-15 | `widget.define` | 校验并返回 v2 入口描述符；核对 render/view 二选一、回调类型和无效定义拒绝。 | v2 | — |  |  |
| W-16 | `widget.apiInfo` | 返回 API 版本、支持版本和 feature ID；核对内容与契约导出一致。 | v2 | — |  |  |
| W-17 | `widget.hasFeature` | 查询稳定 feature ID 是否可用；核对已支持、未知 ID 和降级分支。 | v2 | — |  |  |

## 5. `system` 系统环境 API（3 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| SYS-01 | `system.info` | 获取受限系统环境摘要；核对操作系统、区域及隐私边界。 | v2 | — |  |  |
| SYS-02 | `system.capabilities` | 查询全部或指定系统能力、数据主题和任务的可用/授权状态。 | v2 | — |  |  |
| SYS-03 | `system.uptime` | 获取系统已运行时长；核对单位、单调增长和休眠恢复行为。 | v2 | — |  |  |

## 6. `time` 日期时间 API（6 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| T-01 | `time.now` | 获取当前 UTC epoch 毫秒；核对预览固定时钟和真实宿主时间。 | v2 | — |  |  |
| T-02 | `time.monotonic` | 获取不受系统时间回拨影响的单调时间；核对递增和单位。 | v2 | — |  |  |
| T-03 | `time.parts` | 将时间拆分为指定时区/本地日期字段；核对闰年、月界和时区偏移。 | v2 | — |  |  |
| T-04 | `time.format` | 按 locale、时区和日期/时间样式格式化时间；核对区域差异和无效选项。 | v2 | — |  |  |
| T-05 | `time.add` | 按日历字段增加或减少时间；核对月末、闰日、夏令时和负值。 | v2 | — |  |  |
| T-06 | `time.compare` | 比较两个时间值；核对小于、等于、大于及无效输入。 | v2 | — |  |  |

## 7. `module` 包内模块 API（1 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| M-01 | `module.require` | 仅从组件包 `modules/` 安全加载 Lua 模块；核对缓存、路径穿越、循环依赖和错误隔离。 | v2 | — |  |  |

## 8. `resource` 包内资源 API（4 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| R-01 | `resource.exists` | 查询 manifest 声明的包内资源是否存在；核对存在、缺失、未声明和类型不符。 | v2 | — |  |  |
| R-02 | `resource.image` | 获取已声明图片的不透明资源句柄；核对加载阶段、配额、解码失败和句柄隔离。 | v2 | — |  |  |
| R-03 | `resource.font` | 获取已声明字体的不透明资源句柄；核对加载、字体应用、配额和失败状态。 | v2 | — |  |  |
| R-04 | `resource.status` | 查询图片或字体句柄状态；核对 ready/error、错误码和无效句柄。 | v2 | — |  |  |

## 9. `ui` 即时宿主 UI API（2 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| U-01 | `ui.menu` | 打开宿主管理的菜单并返回选择；核对可信手势、禁用项、取消和动作投递。 | v2 | — |  |  |
| U-02 | `ui.metrics` | 获取页面 CU 语义尺寸；核对 `layoutRowHeight` 直接表示搜索框、普通按钮和单行条目的可见内容高度，外边距与行间距由组件单独组合；核对它对字体、间距、图标和控件的联动、页面 CU 与文本缩放。一至两行使用页面基准，更高组件只随纵向跨度缓慢增长；同纵向跨度一致，横向跨度不得影响高度。`titleAreaHeight` 仅保留旧版外层带高度语义用于兼容。 | v2 | — |  |  |

## 10. `control` 即时输入控件 API（4 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| C-01 | `control.textInput` | 在即时 surface 上使用宿主单行输入代理；核对输入、storage 绑定、提交和取消。 | v2 | — |  |  |
| C-02 | `control.textArea` | 在即时 surface 上使用宿主多行输入代理；核对换行、滚动、绑定和提交。 | v2 | — |  |  |
| C-03 | `control.focus` | 请求宿主输入控件/声明式元素获得焦点；核对可信手势、目标有效性和焦点事件。 | v2 | — |  |  |
| C-04 | `control.blur` | 清除当前宿主输入焦点；核对提交/取消策略、焦点事件和重复调用。 | v2 | — |  |  |

## 11. `calendar` 日历辅助 API（3 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| CAL-01 | `calendar.selectDate` | 打开宿主日期选择并返回结果；核对选择、取消、边界日期和 surface 行为。 | v2 | — |  |  |
| CAL-02 | `calendar.dateInfo` | 获取指定日期的年、月、日、星期等信息；核对闰年和非法日期。 | v1 | — |  |  |
| CAL-03 | `calendar.addDays` | 对日期增加或减少天数；核对跨月、跨年、闰日和负偏移。 | v1 | — |  |  |

## 12. `layout` 尺寸换算 API（21 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| L-01 | `layout.width` | 获取当前 surface 兼容逻辑宽度；核对缩放和 surface 切换。 | v1 | — |  |  |
| L-02 | `layout.height` | 获取当前 surface 兼容逻辑高度；核对缩放和 surface 切换。 | v1 | — |  |  |
| L-03 | `layout.contentWidth` | 获取与声明式根内容框同坐标系的内容宽度。 | v2 | — |  |  |
| L-04 | `layout.contentHeight` | 获取与声明式根内容框同坐标系的内容高度。 | v2 | — |  |  |
| L-05 | `layout.vw` | 将百分比换算为内容宽度单位；核对 0、100、负值和超范围值。 | v2 | — |  |  |
| L-06 | `layout.vh` | 将百分比换算为内容高度单位；核对 0、100、负值和超范围值。 | v2 | — |  |  |
| L-07 | `layout.vmin` | 按内容宽高较小值换算百分比单位；核对横竖尺寸变化。 | v2 | — |  |  |
| L-08 | `layout.vmax` | 按内容宽高较大值换算百分比单位；核对横竖尺寸变化。 | v2 | — |  |  |
| L-09 | `layout.rpx` | 按 manifest 默认跨度的标准短边缩放设计坐标；核对默认尺寸恒等、同宽高比线性缩放、DPI 和非法数值。 | v2 | — |  |  |
| L-10 | `layout.rpxX` | 按 manifest 默认跨度的标准内容宽度缩放横向设计坐标；核对宽度独立缩放和非法数值。 | v2 | — |  |  |
| L-11 | `layout.rpxY` | 按 manifest 默认跨度的标准内容高度缩放纵向设计坐标；核对高度独立缩放、固定底栏占位和非法数值。 | v2 | — |  |  |
| L-12 | `layout.columns` | 获取当前组件占用的网格列数；核对 resize 后更新。 | v1 | — |  |  |
| L-13 | `layout.rows` | 获取当前组件占用的网格行数；核对 resize 后更新。 | v1 | — |  |  |
| L-14 | `layout.sizeClass` | 获取 small/medium/large 尺寸分类；核对分类边界和 resize。 | v1 | — |  |  |
| L-15 | `layout.cellWidth` | 获取单个桌面网格单元宽度；核对 DPI 与布局设置变化。 | v1 | — |  |  |
| L-16 | `layout.cellHeight` | 获取单个桌面网格单元高度；核对 DPI 与布局设置变化。 | v1 | — |  |  |
| L-17 | `layout.cellScale` | 获取相对基准网格单元的缩放值；核对不同 DPI/网格设置。 | v1 | — |  |  |
| L-18 | `layout.cu` | 将网格相对单位换算为逻辑长度；核对正负值和缩放一致性。 | v1 | — |  |  |
| L-19 | `layout.fontCu` | 将网格相对单位换算为字体尺寸；核对文本缩放与 DPI。 | v1 | — |  |  |
| L-20 | `layout.cellGap` | 获取桌面网格单元间距；核对布局设置变化。 | v1 | — |  |  |
| L-21 | `layout.barHeight` | 获取启用组件底栏时的兼容高度；核对标题栏显示配置。 | v1 | — |  |  |

## 13. `storage` 持久化存储 API（5 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| ST-01 | `storage.get` | 读取实例持久化键值；核对缺失键、默认值、类型还原和实例隔离。 | v1 | — |  |  |
| ST-02 | `storage.set` | 写入实例持久化键值；核对支持类型、额度、覆盖和重启后保留。 | v1 | — |  |  |
| ST-03 | `storage.remove` | 删除实例持久化键；核对存在/缺失键和重启后结果。 | v1 | — |  |  |
| ST-04 | `storage.keys` | 列出实例存储键；核对排序/稳定性、增删同步和实例隔离。 | v1 | — |  |  |
| ST-05 | `storage.transaction` | 原子执行一组持久化读写；核对提交、异常回滚、嵌套限制和写入额度。 | v2 | — |  |  |

## 14. `slots` 宿主管理逻辑槽位 API（6 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| SLO-01 | `slots.binding` | 读取/操作 manifest 声明的单值绑定槽；核对快照、选择、清除和类型限制。 | v2 | — |  |  |
| SLO-02 | `slots.collection` | 读取/操作 manifest 声明的集合槽；核对顺序、增删、选择、容量和修订号。 | v2 | — |  |  |
| SLO-03 | `slots.canUndo` | 查询当前槽位操作是否可撤销；核对空历史和新操作后的变化。 | v2 | — |  |  |
| SLO-04 | `slots.canRedo` | 查询当前槽位操作是否可重做；核对撤销前后和新分支操作。 | v2 | — |  |  |
| SLO-05 | `slots.undo` | 在可信用户动作中撤销槽位修改；核对拒绝路径、修订号和事件。 | v2 | — |  |  |
| SLO-06 | `slots.redo` | 在可信用户动作中重做槽位修改；核对拒绝路径、修订号和事件。 | v2 | — |  |  |

## 15. `state` VM 瞬态状态 API（6 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| STA-01 | `state.get` | 读取当前实例 VM 的瞬态值；核对缺失键、默认值和深拷贝。 | v2 | — |  |  |
| STA-02 | `state.set` | 写入当前实例 VM 的 JSON-like 瞬态值；核对类型、额度和覆盖。 | v2 | — |  |  |
| STA-03 | `state.remove` | 删除瞬态键；核对存在/缺失键和返回结果。 | v2 | — |  |  |
| STA-04 | `state.has` | 判断瞬态键是否存在；核对值为 false 时仍能正确区分存在性。 | v2 | — |  |  |
| STA-05 | `state.keys` | 列出瞬态键；核对增删同步、顺序稳定性和实例隔离。 | v2 | — |  |  |
| STA-06 | `state.clear` | 清空当前 VM 的全部瞬态状态；核对其他实例和持久化 storage 不受影响。 | v2 | — |  |  |

## 16. `schedule` 调度 API（5 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| SCH-01 | `schedule.every` | 注册周期计划并产生 schedule 事件；核对周期、隐藏策略、合并和重复 ID。 | v2 | — |  |  |
| SCH-02 | `schedule.after` | 注册一次性延迟计划；核对触发时间、取消、隐藏和卸载回收。 | v2 | — |  |  |
| SCH-03 | `schedule.at` | 注册绝对 UTC 时间计划；核对过去时间、时钟变化、触发和取消。 | v2 | — |  |  |
| SCH-04 | `schedule.timeline` | 注册带多个时间点的时间线；核对顺序、载荷、结束标记、遗漏合并和取消。 | v2 | — |  |  |
| SCH-05 | `schedule.cancel` | 按 ID 取消计划；核对存在/未知 ID、重复取消和事件不再触发。 | v2 | — |  |  |

## 17. `animation` 帧动画 API（2 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| A-01 | `animation.requestFrame` | 请求下一次宿主动画帧事件；核对单次触发、帧时间、合并和 reducedMotion。 | v2 | — |  |  |
| A-02 | `animation.cancelFrame` | 取消尚未触发的动画帧请求；核对已触发/未知 ID 和卸载回收。 | v2 | — |  |  |

## 18. `data` 系统数据订阅 API（1 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| DAT-01 | `data.subscribe` | 订阅系统数据 topic 并读取状态包络；逐项核对权限、参数、采样、缓存、拒权、错误和变更事件。 | v2 | 依 topic |  |  |

## 19. `task` 异步任务 API（2 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| TASK-01 | `task.start` | 启动系统契约声明的异步任务；逐项核对参数、权限、可信手势、并发限制和完成事件。 | v2 | 依任务 |  |  |
| TASK-02 | `task.cancel` | 取消当前实例启动的异步任务；核对运行中/已完成/未知 ID 和最终事件。 | v2 | — |  |  |

## 20. `l10n` 本地化 API（7 项）

| 编号 | API | 中文说明与验收重点 | 引入版本 | 权限 | 结果 | 证据 / 备注 |
|---|---|---|---:|---|---|---|
| LOC-01 | `l10n.tr` | 按键读取组件本地化文本并插值；核对各语言、缺失键回退和占位参数。 | v1 | — |  |  |
| LOC-02 | `l10n.language` | 获取当前组件语言标签；核对语言切换和 BCP 47 格式。 | v1 | — |  |  |
| LOC-03 | `l10n.formatNumber` | 按当前 locale 格式化数字；核对小数、分组、负数和边界值。 | v2 | — |  |  |
| LOC-04 | `l10n.formatBytes` | 将字节数格式化为本地化容量文本；核对单位切换、精度和负值。 | v2 | — |  |  |
| LOC-05 | `l10n.formatDuration` | 将毫秒时长格式化为短文本或时钟格式；核对零值、长时长和负值。 | v2 | — |  |  |
| LOC-06 | `l10n.formatRelativeTime` | 将目标时间格式化为本地化相对时间；核对过去、未来、当前和边界单位。 | v2 | — |  |  |
| LOC-07 | `l10n.formatList` | 按 locale 连接字符串列表；核对空、单项、双项、多项及连接词。 | v2 | — |  |  |

## 21. 验收汇总

| 分类 | 总数 | P | F | B | N/A | 负责人 / 备注 |
|---|---:|---:|---:|---:|---:|---|
| `draw` | 19 |  |  |  |  |  |
| `interaction` | 6 |  |  |  |  |  |
| `view` | 48 |  |  |  |  |  |
| `widget` | 17 |  |  |  |  |  |
| `system` | 3 |  |  |  |  |  |
| `time` | 6 |  |  |  |  |  |
| `module` | 1 |  |  |  |  |  |
| `resource` | 4 |  |  |  |  |  |
| `ui` | 2 |  |  |  |  |  |
| `control` | 4 |  |  |  |  |  |
| `calendar` | 3 |  |  |  |  |  |
| `layout` | 21 |  |  |  |  |  |
| `storage` | 5 |  |  |  |  |  |
| `slots` | 6 |  |  |  |  |  |
| `state` | 6 |  |  |  |  |  |
| `schedule` | 5 |  |  |  |  |  |
| `animation` | 2 |  |  |  |  |  |
| `data` | 1 |  |  |  |  |  |
| `task` | 2 |  |  |  |  |  |
| `l10n` | 7 |  |  |  |  |  |
| **合计** | **168** |  |  |  |  |  |

最终结论：`□ 通过`　`□ 有条件通过`　`□ 不通过`

遗留问题、风险与发布限制：

1. （待填写）
2. （待填写）
3. （待填写）

# 启动图标与 Dock 弹窗审查项修正（2026-09-23）

用户在[相关改动审查](startup_dock_related_review_20260923.md)后要求修复三个发现。基线为 `a3000a99`，分支为 `release/v1.0.7.0`。本轮未修改公共组件 API、布局 schema、版本号或用户文件。

## 调整

- **DOCK-POPUP-01**：区分已确定的映射路径和来源版本已变化的快捷方式目标。过期目标保留 Dock 分区身份但不再暴露旧路径；弹窗保持加载、禁用目录操作。当前版本的目标完成后，仅接续同一来源且仍可交互的弹窗；重新绑定时取消旧取图、清除旧条目，旧路径快照不能重新启用操作。目录内容读取仍独立判断可访问性，普通映射路径不等待无关分类。
- **STARTUP-ICON-01**：分类阶段提交失败后保留待办，消费完成结果后分批重试。待办取消/停止与各图标阶段一起清理，重用同名请求不会恢复取消的旧结果；待办达到容量后停止接纳新的首图请求，由已有宿主维护路径重试，避免无界增长。默认工作线程数和每阶段容量保持不变。
- **DOCK-POPUP-02**：本次打开期间独立记住已知内容数量，只用于几何。加载结束为空/失败时继续保留相同网格/列表占位尺寸；实际条目数、点击和导航仍使用真实数据。关闭、切换弹窗时清理该预留，下一次打开可用新的空目录尺寸；非空首批内容仍沿用已有 reveal。

## 定向证据

- 隔离验证直接调用生产 `Work/BackgroundWork`、目标绑定/读取 helper 和布局函数。只替换提供程序等待与目录快照，不替换调度或提交逻辑。
- 生产调度器原容量压力场景：2050 个首图、2050 个分类全部交付；对照审查时为 2050/2048。命令 `python .codex-probes/related-fixes-20260923/stress.py`，退出 0，5.735 秒，未查询或改动用户文件。
- 永久回归加入现有 `slot_runtime_contract`，覆盖队列满载、容量恢复、被拒首图重试、取消后重用同名请求、停止释放待办，以及 A→B 目标重绑定与旧结果拒绝；`dock_and_window_rules` 补充空状态网格/列表尺寸交接及重开释放预留。
- 隔离副本负向对照命令 `python .codex-probes/related-fixes-20260923/negative.py`：候选退出 0；撤销待办保留、取消清理、旧目标拦截、空状态尺寸保持分别退出 1，并触发对应行为断言。没有以编译错误或超时作为负向成功；编译日志无编译/链接警告。副本只位于 `.codex-probes/`，生产源码未回退。
- 输入清单覆盖 1,094 个源码/构建/测试/资源文件，SHA-256：`80cc35d93b98933db943042a59bef55d7d3b6a82ba9bd0b03795736cef3db994`。完整本地日志和清单位于 `.codex-probes/related-fixes-20260923/`，不作为产品依赖。

## 构建及回归检查点

- `scripts/build.bat --reload-shell`：退出 0，485.969 秒，标准 Release 产物已生成，无编译/链接警告。运行时间 2026-09-23T10:56:32.991389+08:00 至 2026-09-23T11:04:38.967336+08:00；EXE SHA-256 `943225cc41c597410bb82e0f0582f93fad6ac06f50676a267293657e94b9d723`。
- 按构建前进程/Hook 占用检查，已告知并停止宿主、重启 Explorer。脚本中的 `timeout /t 2` 在重定向输入下提示不支持输入重定向；后续配置、编译、链接和输出整理正常完成，该提示不是编译/链接诊断。
- 此编译检查点已执行上面的隔离回归与负向对照；仓库定向/完整测试将在同一候选上继续执行。尚未运行的测试和桌面视觉验收不计为通过。

## 证据边界

没有启动、捕获或自动操作桌面宿主，没有更改真实快捷方式目标或执行粘贴/新建。快捷方式目标绑定的自动化证据仍止于生产缓存/状态/读取边界；空目录和失败状态的实际动画中间帧需用户使用新开发版确认。本轮三个发现先记录为已调整，其中分类队列错误在受控输入上已纠正；不以编译和规则回归代替完整桌面视觉验收。


## 最终自动化检查点

- 代码候选：`ee0daf28c98e88703961369ad4b82c9aca21d809`，编译后的运行输入没有继续修改；上面 1,094 项输入已再次逐项核对一致。
- 本次执行 `scripts/test.bat name "^(slot_runtime_contract|dock_and_window_rules|popup_animation_rules)$"`：退出 0，**3/3**，含 2 项 integration；31.844 秒含配置/构建，CTest 2.15 秒。报告 `D:\Code\Other\SnowDesktop\.build\Testing\test-run-1965ebdb2e364aabacbab0495a0715e5.xml`。
- 本次执行 `scripts/test.bat full`：退出 0，**120/120**；137.047 秒含配置/构建，CTest 111.91 秒；2026-09-23T11:05:39.241741+08:00 至 2026-09-23T11:07:56.290803+08:00。报告 `D:\Code\Other\SnowDesktop\.build\Testing\test-run-0bacb453cb57423cba5312c73defda62.xml`。默认排除的手动诊断未计入通过。
- 两次测试构建完整日志均无编译/链接警告。完整测试最终产物 EXE SHA-256：`335d50481d3326da30956966159f2c98f14dd893f28cc42a37dcddd415c77ef5`；标准构建产物哈希见前文，二者绑定相同运行输入。
- 文档相对链接、差异检查和原有未提交修改保留检查通过；没有推送远程。视觉验收边界仍按前文保留。

### 核心拖动操作
| 源\目标 | 桌面（GridContainer）       | 集合/桌面文件组件（ListContainer） | 映射文件夹（ListContainer） | 外部      |
| ----------------- | --------------------------- | ---------------------------------- | --------------------------- | --------- |
| 桌面              | A位置选取+位置移动          | B位置选取+位置移动                 | C位置选取+位置移动+文件操作 | D文件操作 |
| 集合/桌面文件组件 | E位置选取+位置移动          | F位置移动                          | G1位置选取+位置移动+文件操作 | H文件操作 |
| 映射文件夹        | I位置选取+位置移动+文件操作 | G2位置选取+位置移动+文件操作       | K位置移动+文件操作          | L文件操作 |
| 外部              | M位置选取+位置移动+文件操作 | N位置选取+位置移动+文件操作        | O位置移动+文件操作          |           |

> 第一列是操作源
>
> G1 和 G2 原始文档均为 G，为区分测试语境标记为 G1（集合→映射）和 G2（映射→集合）

- 操作源分为单个item（A）和多个item（B）
- 位置选取是为单个item和多个item分配单个或多个slot
- 位置移动指的是布局中item的移动
- 文件操作分移动（A）、复制（B）、创建快捷方式（C），其中不涉及外部和映射文件夹的要自己处理复制和创建快捷方式的后缀添加，涉及映射文件夹和外部的无需添加后缀

### 测试编码规则

每条测试用三位编码表示：`[单元格][源数量][操作类型]`

- **第一位**：单元格字母（A~O，G1/G2）
- **第二位**：A=单个item, B=多个item
- **第三位**：A=移动, B=复制, C=创建快捷方式

注意：不涉及外部和映射文件夹的单元格（A/B/E/F），复制和创建快捷方式需要自行处理后缀添加；涉及映射文件夹和外部目标的无需添加后缀。

---

### 测试明细

所有单元格统一按源数量（行）× 操作类型（列）的二维表测试。

#### A 桌面 → 桌面（位置选取+位置移动）

| 源数量 | 移动 (A) | 复制 (B) | 创建快捷方式 (C) |
|---|---|---|---|
| 单个item (A) | AAA：没问题 | AAB：目标落点很混乱 | AAC：目标落点很混乱 |
| 多个item (B) | ABA：没问题 | ABB：目标落点很混乱 | ABC：目标落点很混乱 |

代码审查标注：

- ABB/ABC 的“先按修饰键无法拖动”能从代码解释：`OnLeftButtonDown` 在 Ctrl 下会先切换选中态，后续 `OnMouseMove` 又要求 `mouseDownHit_->IsSelected()` 才进入拖动，导致按住 Ctrl 从已选项起拖时可能被取消选中。
- AAB/AAC/ABB/ABC 依赖 `DropFilePathsToDesktopCell`，普通文件会追加“副本”或生成 `.lnk`；受保护桌面图标没有文件路径，会被静默跳过。
- 桌面拖动预览和实际落点存在不一致风险：预览用当前鼠标点算格子，实际移动用拖动组左上角偏移后的点。

#### B 桌面 → 集合/桌面文件组件（位置选取+位置移动）

| 源数量 | 移动 (A) | 复制 (B) | 创建快捷方式 (C) |
|---|---|---|---|
| 单个item (A) | BAA：待测试 | BAB：待测试 | BAC：待测试 |
| 多个item (B) | BBA：待测试 | BBB：待测试 | BBC：待测试 |

代码审查标注：

- 集合目标：BAA/BBA 走 key 移动，BAB/BBB/BAC/BBC 先在桌面物化副本或快捷方式，再把新 key 插入集合；位置插入使用 `targetSlot`，但物化阶段临时落到组件自身格子，存在布局保存/闪现风险。
- 桌面文件组件目标：移动会按分类收纳；复制同样先物化再收纳；创建快捷方式会生成 `.lnk`，但 `IsCollectable` 排除了 `.lnk`，所以 BAC/BBC 对桌面文件组件目标大概率表现为桌面生成了快捷方式、组件内没有出现。
- 该块和 F/N 的物化逻辑在 `Collection`、`FileCategories` 中重复实现，后续应抽成对象化的“桌面物化后插入组件”服务。

#### C 桌面 → 映射文件夹（位置选取+位置移动+文件操作）

| 源数量 | 移动 (A) | 复制 (B) | 创建快捷方式 (C) |
|---|---|---|---|
| 单个item (A) | CAA：待测试 | CAB：待测试 | CAC：待测试 |
| 多个item (B) | CBA：待测试 | CBB：待测试 | CBC：待测试 |

代码审查标注：

- 文件操作已接入：移动/复制通过 `SHFileOperationW` 到映射目录，快捷方式通过 `IShellLinkW` 创建。
- 位置选取只用于命中目标容器，`targetSlot` 没有参与映射目录内的最终顺序；CAA/CBA/CAB/CBB/CAC/CBC 都缺少“落到指定位置”的持久语义。
- 文件操作结果没有统一校验；移动失败时仍可能继续刷新和清理来源 widget key，需重构为带结果的文件操作对象。

#### D 桌面 → 外部（文件操作）

| 源数量 | 移动 (A) | 复制 (B) | 创建快捷方式 (C) |
|---|---|---|---|
| 单个item (A) | DAA：待测试 | DAB：待测试 | DAC：待测试 |
| 多个item (B) | DBA：待测试 | DBB：待测试 | DBC：待测试 |

代码审查标注：

- 拖出外部使用 `CreateSelectedDataObject` 从桌面 Shell PIDL 生成 `IDataObject`，桌面普通 item 的 DAA/DBA/DAB/DBB/DAC/DBC 链路基本存在。
- 外部 Drop 完成后只有 `DROPEFFECT_MOVE` 会删除源文件，且删除逻辑跳过受保护桌面图标；实际 Ctrl/Alt/Shift 效果由目标窗口返回的 `DROPEFFECT` 决定，需要交互实测。

#### E 集合/桌面文件组件 → 桌面（位置选取+位置移动）

| 源数量 | 移动 (A) | 复制 (B) | 创建快捷方式 (C) |
|---|---|---|---|
| 单个item (A) | EAA：待测试 | EAB：待测试 | EAC：待测试 |
| 多个item (B) | EBA：待测试 | EBB：待测试 | EBC：待测试 |

代码审查标注：

- 集合/桌面文件组件内部 item 本质仍是桌面 `DesktopItem`，移动到桌面会先从所有 widget key 中移除，再按目标格移动；EAA/EBA 链路存在。
- EAB/EBB/EAC/EBC 通过 `DropFilePathsToDesktopCell` 物化副本或快捷方式，复制会加“副本”后缀。
- 多选顺序依赖来源容器 `GetSelectedItems` 返回顺序；集合较稳定，桌面文件组件在分类过滤状态下需要确认跨分类选中是否会丢项。

#### F 集合/桌面文件组件 → 集合/桌面文件组件（位置移动）

| 源数量 | 移动 (A) | 复制 (B) | 创建快捷方式 (C) |
|---|---|---|---|
| 单个item (A) | FAA：待测试 | FAB：待测试 | FAC：待测试 |
| 多个item (B) | FBA：待测试 | FBB：待测试 | FBC：待测试 |

代码审查标注：

- 同一集合内移动走 `ReorderMembers`，跨集合移动走 key 转移；FAA/FBA 对集合目标链路存在。
- 同一桌面文件组件内移动调用 `ReorderMembers`，但实现基于当前激活分类可见项，跨分类或多选时有丢失/错序风险。
- FAB/FBB/FAC/FBC 会物化新桌面文件再插入目标组件；桌面文件组件目标的快捷方式仍会被 `.lnk` 过滤，属于确定缺口。
- 该块的集合和桌面文件组件逻辑现在由两个类各写一遍，后续应抽为共享的 keyed-widget drop strategy，而不是继续扩过程分支。

#### G1 集合/桌面文件组件 → 映射文件夹（位置选取+位置移动+文件操作）

| 源数量 | 移动 (A) | 复制 (B) | 创建快捷方式 (C) |
|---|---|---|---|
| 单个item (A) | G1AA：待测试 | G1AB：待测试 | G1AC：待测试 |
| 多个item (B) | G1BA：待测试 | G1BB：待测试 | G1BC：待测试 |

代码审查标注：

- 文件操作链路存在：来源 item 的路径会被移动/复制/创建快捷方式到映射目录。
- G1AA/G1BA 移动后会从所有 widget key 中移除来源 key；但当前没有等文件操作成功后再清理，失败时可能误删组件归属。
- `targetSlot` 被忽略，映射文件夹内没有“落点顺序”持久化；所有 G1 用例都缺少位置移动语义。

#### H 集合/桌面文件组件 → 外部（文件操作）

| 源数量 | 移动 (A) | 复制 (B) | 创建快捷方式 (C) |
|---|---|---|---|
| 单个item (A) | HAA：待测试 | HAB：待测试 | HAC：待测试 |
| 多个item (B) | HBA：待测试 | HBB：待测试 | HBC：待测试 |

代码审查标注：

- 集合/桌面文件组件 item 仍对应 `items_` 中的桌面 PIDL，拖出外部可复用 `CreateSelectedDataObject`，普通文件链路基本存在。
- 外部移动成功后通过 `ReloadItems` 间接清理丢失的桌面文件和 widget key，缺少显式的来源组件清理结果对象；HAA/HBA 需要重点实测。
- 快捷方式由外部目标决定是否支持 `DROPEFFECT_LINK`，本应用只暴露可用效果，没有自建 `.lnk`。

#### I 映射文件夹 → 桌面（位置选取+位置移动+文件操作）

| 源数量 | 移动 (A) | 复制 (B) | 创建快捷方式 (C) |
|---|---|---|---|
| 单个item (A) | IAA：待测试 | IAB：待测试 | IAC：待测试 |
| 多个item (B) | IBA：待测试 | IBB：待测试 | IBC：待测试 |

代码审查标注：

- 映射文件夹条目通过 `FolderEntryIcon::GetPath` 提供路径，落到桌面时走 `DropFilePathsToDesktopCell`，移动/复制/快捷方式链路存在。
- IAA/IBA 移动成功后会刷新来源映射组件；但桌面目标侧没有以统一结果对象确认成功，只用布尔值决定刷新。
- 涉及映射文件夹来源时复制不追加“副本”后缀，符合当前规则。

#### G2 映射文件夹 → 集合/桌面文件组件（位置选取+位置移动+文件操作）

| 源数量 | 移动 (A) | 复制 (B) | 创建快捷方式 (C) |
|---|---|---|---|
| 单个item (A) | G2AA：待测试 | G2AB：待测试 | G2AC：待测试 |
| 多个item (B) | G2BA：待测试 | G2BB：待测试 | G2BC：待测试 |

代码审查标注：

- 目标为集合时，会先把映射文件夹条目物化到桌面，再把新桌面 key 插入集合，位置插入使用 `targetSlot`。
- 目标为桌面文件组件时，创建快捷方式生成 `.lnk` 后会被 `IsCollectable` 排除，G2AC/G2BC 对该目标是确定缺口。
- 移动来源为映射文件夹时会刷新来源映射组件；但目标物化和来源刷新没有统一事务，失败清理语义不清楚。

#### K 映射文件夹 → 映射文件夹（位置移动+文件操作）

| 源数量 | 移动 (A) | 复制 (B) | 创建快捷方式 (C) |
|---|---|---|---|
| 单个item (A) | KAA：待测试 | KAB：待测试 | KAC：待测试 |
| 多个item (B) | KBA：待测试 | KBB：待测试 | KBC：待测试 |

代码审查标注：

- 同一映射文件夹内 `origin == this` 直接 `ReorderMembers` 后返回，忽略 Ctrl/Alt/Shift；KAB/KBB/KAC/KBC 在同源场景代码上不会复制或创建快捷方式。
- KAA/KBA 的同源移动只是内存里调整 `folderEntries` 顺序，没有文件系统或布局持久化，刷新后大概率丢失。
- 不同映射文件夹之间会执行文件移动/复制/快捷方式，但仍忽略 `targetSlot`，没有目标位置语义。
- K 是当前缺口最大的块，需要专门的 folder-backed drop strategy，而不是在 `FolderMapping::OnItemsDropped` 里继续堆 if。

#### L 映射文件夹 → 外部（文件操作）

| 源数量 | 移动 (A) | 复制 (B) | 创建快捷方式 (C) |
|---|---|---|---|
| 单个item (A) | LAA：待测试 | LAB：待测试 | LAC：待测试 |
| 多个item (B) | LBA：待测试 | LBB：待测试 | LBC：待测试 |

代码审查标注：

- 当前拖出外部只调用 `CreateSelectedDataObject`，它只遍历桌面 `items_` 生成 Shell PIDL；映射文件夹的 `FolderEntryIcon` 不在其中。
- 因此 LAA/LBA/LAB/LBB/LAC/LBC 不是单纯待测，而是代码链路缺失：需要能从任意 `Item::GetPath()` 创建文件系统 `IDataObject`。

#### M 外部 → 桌面（位置选取+位置移动+文件操作）

| 源数量 | 移动 (A) | 复制 (B) | 创建快捷方式 (C) |
|---|---|---|---|
| 单个item (A) | MAA：待测试 | MAB：待测试 | MAC：待测试 |
| 多个item (B) | MBA：待测试 | MBB：待测试 | MBC：待测试 |

代码审查标注：

- 外部到桌面非 widget 目标走 Shell 桌面 drop target，随后 `PlaceNewItemsAtDropPoint` 按落点摆放新增桌面项，位置选取链路存在。
- Move/Copy/Link 的最终效果由外部数据对象和 Shell drop target 协商，应用侧没有自己的统一 drop action 结果；M 组需要完整实测。

#### N 外部 → 集合/桌面文件组件（位置选取+位置移动+文件操作）

| 源数量 | 移动 (A) | 复制 (B) | 创建快捷方式 (C) |
|---|---|---|---|
| 单个item (A) | NAA：待测试 | NAB：待测试 | NAC：待测试 |
| 多个item (B) | NBA：待测试 | NBB：待测试 | NBC：待测试 |

代码审查标注：

- 外部到 widget 目标会构造 `ExternalFileItem` 并调用目标 `OnItemsDropped`，集合目标的复制/移动/快捷方式插入链路存在。
- 桌面文件组件目标的 NAC/NBC 快捷方式仍会被 `.lnk` 过滤，确定有缺口。
- 外部默认动作是复制；Shift 移动、Alt 快捷方式由 `DropActionFromMods` 支持，但物化成功和插入成功没有统一事务结果。

#### O 外部 → 映射文件夹（位置移动+文件操作）

| 源数量 | 移动 (A) | 复制 (B) | 创建快捷方式 (C) |
|---|---|---|---|
| 单个item (A) | OAA：待测试 | OAB：待测试 | OAC：待测试 |
| 多个item (B) | OBA：待测试 | OBB：待测试 | OBC：待测试 |

代码审查标注：

- 外部到映射文件夹通过 `ExternalFileItem` 路径进入 `FolderMapping::OnItemsDropped`，复制/移动/快捷方式文件操作链路存在。
- `targetSlot` 被忽略，OAA/OBA/OAB/OBB/OAC/OBC 都没有映射目录内位置语义。
- 文件操作没有返回可消费的成功/失败明细，失败时仍会刷新目标；需要纳入统一文件操作结果。

### 渲染类问题



### 菜单问题

1.列表显示和图标显示的切换：在列表模式下右键应该是图标显示字样和图标，现在始终为列表显示，容易误导

2.自动收集在菜单前应该是对勾，要不不知道状态

### 其他问题

1. 新组件没做占位检测，会重叠
2. 组件底栏对移动太敏感，点击就虚化为位置指示器了

### 设置的引入



### 自定义组件引擎的引入

 


# Dock 悬浮窗口设计与交互约束

## 背景

SnowDesktop 曾尝试通过 AppBar 预留工作区，并使用覆盖整个虚拟桌面的
置顶合成窗口承载 Dock。该方案能够让最大化窗口避开 Dock，但同时把
任务栏、系统菜单、输入区域、多显示器、DPI 和合成窗口层级耦合在一起。

实践中出现过以下问题：

- 全屏合成窗口的区域初始化或更新失败时，可能遮挡其他应用输入。
- `HTTRANSPARENT` 和 `WS_EX_TRANSPARENT` 不能提供可靠的跨进程点击穿透。
- Dock、任务栏、系统右键菜单和集合弹窗之间难以维持稳定的 Z 序。
- 弹窗启闭需要重建合成区域，容易造成 Dock 闪烁。
- 全局低级鼠标钩子会进入每次鼠标移动的系统热路径，拖动体验明显下降。
- AppBar、自动隐藏任务栏和全屏应用状态变化会触发大量窗口重排。

因此后续不再把“永久置顶并预留工作区”作为 Dock 的主要实现方向。

## 目标架构

Dock 保留两种展示状态，但共享同一份数据模型、布局和绘制逻辑：

1. **桌面 Dock**
   - 由现有桌面宿主承载。
   - 默认可见，不置顶。
   - 可以继续占用 SnowDesktop 自身的图标布局区域，但不注册 Windows AppBar，
     不改变其他应用的系统工作区。
   - 普通应用和最大化窗口可以自然覆盖它。

2. **快捷悬浮 Dock**
   - 通过快捷键把当前桌面 Dock 切换为悬浮宿主；再次触发后切回桌面宿主。
   - 桌面宿主与悬浮宿主不能同时绘制 Dock。
   - 严格复用桌面 Dock 的位置与尺寸，不重新计算一套悬浮布局。
   - 使用与 Dock 实际边界一致的紧凑窗口，不创建全虚拟桌面窗口。
   - 不注册 AppBar，不修改 Explorer 任务栏窗口。

## 窗口样式与层级

悬浮 Dock 使用独立的 `WS_POPUP` 窗口，并采用：

```text
WS_EX_TOOLWINDOW
WS_EX_NOACTIVATE
WS_EX_NOREDIRECTIONBITMAP
```

悬浮 Dock 不使用永久 `WS_EX_TOPMOST`。由于 `WS_EX_NOACTIVATE`
窗口可能被前台进程限制在其下方，显示时允许执行一次
`HWND_TOPMOST → HWND_NOTOPMOST` 的瞬时提升，最终立即回到普通窗口层顶部。
这样能覆盖快捷键触发时的当前应用，又不会长期压住任务栏、系统菜单和后续
出现的系统界面。毛玻璃辅助窗口必须采用相同的非 TOPMOST 最终状态。

窗口层级必须满足：

```text
系统任务栏 / 系统菜单
原生右键菜单
集合弹窗
拖拽虚影（仅拖动期间）
悬浮 Dock
普通应用窗口
桌面 Dock
```

这里的拖拽虚影只需要高于悬浮 Dock，不得进入永久 TOPMOST 层。

## 集合弹窗约束

- 集合弹窗与 Dock 共用一个紧凑悬浮宿主，并在 Dock 之后绘制。
- 宿主窗口区域是 Dock 输入区域与集合弹窗区域的并集；两者之间的透明空白
  不得接收输入。
- 集合弹窗必须位于 Dock 绘制层上方，但仍低于原生菜单。
- Dock 隐藏、显示器切换或布局失效时，集合弹窗同步关闭。
- 再次点击当前集合按钮时关闭弹窗，不得先关闭后又在按钮释放阶段重开。
- 点击另一个集合按钮时直接切换内容，避免隐藏并重建整个 Dock。
- 集合弹窗不依赖全屏窗口区域裁剪。

## 点击与关闭状态机

悬浮 Dock 的显示状态由明确事件驱动：

- 快捷键：隐藏时显示，显示时关闭。
- 点击当前集合按钮：切换关闭集合弹窗。
- 点击其他 Dock 项：先关闭集合弹窗，再执行目标操作。
- 点击其他应用：关闭集合弹窗和悬浮 Dock，不吞掉目标应用的点击。
- 启动 Dock 项：提交启动动作后关闭悬浮 Dock。
- 拖拽开始：暂停鼠标离开和外部点击自动关闭。
- 拖拽结束：恢复关闭判定。
- Explorer 重启、显示器变化或 Dock 禁用：销毁悬浮窗口和全部 owned popup。

禁止使用 `WH_MOUSE_LL` 监视外部点击。若非激活窗口无法通过
`WM_ACTIVATE` 获知点击，可在悬浮窗口可见期间使用短生命周期、低频率的
按钮状态检测；检测不得位于每次鼠标移动的回调中。

## 任务栏共存

- 系统任务栏始终优先，SnowDesktop 不隐藏、不移动、不注入其可见性逻辑。
- 悬浮 Dock 复用桌面 Dock 已计算的位置；该位置基于显示器工作区，因此可见
  任务栏仍位于 Dock 外侧。
- 自动隐藏任务栏状态变化不再改写 Dock 或任务栏 HWND。
- 无法可靠获取任务栏边界时，不提升到永久 TOPMOST 层。

## 性能约束

- 鼠标移动和拖拽热路径不得创建、销毁或重新注册窗口。
- 拖拽热路径不得调用 AppBar 协商、任务栏注入或全局鼠标钩子。
- 静态 Dock 背景应缓存，鼠标移动仅更新动态指示器和拖拽虚影。
- 集合弹窗启闭只允许调整紧凑宿主的区域和合成表面，不得重建 Dock 窗口。
- 仅在布局、DPI、显示器或主题真正变化时重建合成资源。

## 测试要求

- 快捷键显示、隐藏和重复切换。
- 四个屏幕边缘以及负坐标显示器定位。
- 混合 DPI 下的窗口边界和 4px 间距。
- 主任务栏、副任务栏以及自动隐藏任务栏的共存。
- 集合按钮重复点击、集合切换和外部点击关闭。
- 右键菜单、集合弹窗、拖拽虚影和系统任务栏的层级。
- 拖动过程中不重建窗口，并保持指针跟随。
- Explorer 重启、显示器插拔和 Dock 禁用后的资源清理。
